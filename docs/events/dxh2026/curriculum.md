# DXH講座 カリキュラム・進行台本

> **講師・アシスタント向けの内部文書。** 高校教員には配布しない。参加者は各貸出PCに配置したスライドPDF（`docs/events/dxh2026/slides/dxh_workshop.pdf`）を参照する（印刷配布なし・2026-07-16 決定）。`handout.md` は後日公開するガイドの元原稿。

## 0. この文書について

2026-07-18（土）・07-19（日）開催の DXH（高等学校DX加速化推進事業）高校教員向け体験講座（各日定員10名・各2時間）の進行台本を兼ねるカリキュラムをまとめる。講師・アシスタントは当日この文書の時刻・チェックポイントに沿って進行する。

参照文書:

| 文書 | 内容 |
|------|------|
| `docs/events/dxh2026/setup-loaner-pc.md` | 貸出PC・機材のセットアップ手順、リハーサル計画、講座終了後の復元手順 |
| `docs/events/dxh2026/equipment-checklist.md` | 機材数量一覧・前日/当日朝/終了後チェックリスト |
| `firmware/vehicle/docs/operation_manual.md` | リリース vehicle ファームの操作仕様（正） |
| `docs/guides/motor_spin_quickstart.md` §6-3 | ③④で使うモータ Duty ハードコードの実習コード（本書に転記） |
| `docs/guides/safety.md` | 飛行安全ガイド |

**表記ルール:** 未実測・未確認の項目には【リハーサル実測】【リハーサル確認】と明記している。断定できない数値・挙動をこの文書内で創作しないこと。リハーサルで判明した実測値は本文書の該当箇所を直接置き換える。

---

## 1. 講座概要

### 開催情報

| 項目 | 内容 |
|------|------|
| 日程 | 2026-07-18（土）・2026-07-19（日）各日独立開催・同一内容 |
| 時間 | 各日 2時間（120分）1コマ |
| 定員 | 10名／日 |
| 機材 | Windows 11 PC（Core i5）＋ StampFly 本体＋ Atom JoyStick コントローラ＋ USB-Cケーブル。大学貸出・1人1セット（事前ペアリング済み・事前セットアップ済み） |

### 学習目標（告知4テーマに対応）

| # | 告知テーマ | 学習目標 |
|---|-----------|---------|
| ① | ドローン操縦体験 | シミュレータ（`sf sim run`）で操縦感覚を安全に掴んでから、ALT_HOLD（高度維持モード）による実機の自動離陸・スティック操作・自動着陸を体験し、自動制御がもたらす操縦の容易さを実感する |
| ② | 開発環境のインストール | 開発環境の全体像を知り、コマンド（sf CLI）でファームウェアを機体に書き込む流れを体験する。開発環境がない PC でもブラウザだけで書き込める仕組み（Landing ページ・Webフラッシャ）も紹介する |
| ③ | プログラム書き換え体験 | サンプルコードの数値を書き換え → ビルド（ソースコードを実行可能な形式に変換する作業）→ 書き込み、という開発の一連の流れを実機で体験する |
| ④ | モータの制御 | モータ配置と Duty（PWM のオン時間比率。モータの回転の強さを決める値）の関係を理解し、自分で書き換えた値が実機の回転として現れることを確認する |

### 参加者の前提

- プログラミング言語の使用経験が望ましいが、初心者も受け入れる（大学HP告知文に準拠）
- 実習ではキーボードでのコマンド入力を伴う（コマンドプロンプトでの `cd` や `sf` コマンドの実行）
- ドローン操縦・電子工作の経験は不要
- 専門用語（Duty、ARM、ビルド 等）が出るたびに短い説明を添えて進行する（本書の該当箇所にも解説を併記済み）

---

## 2. タイムテーブル

両日とも **13:30 開始・15:30 終了**（確定）。

| 時刻 | 所要 | パート | 内容 |
|---------|------|--------|------|
| 13:30-13:40 | 10分 | オープニング | 挨拶・StampFly/エコシステム紹介・本日の流れ・安全説明 |
| 13:40-14:05 | 25分 | ① 操縦体験（シミュレータ→実機） | 各自 `sf sim run` で仮想飛行→コントローラをESP-NOWへ切替→ALT_HOLDで実機飛行（自動離着陸）。1人1セットで全員同時（交代なし）。機体は事前ペアリング済み |
| 14:05-14:30 | 25分 | ② 開発環境+書き込み体験 | デモ・紹介10分（開発環境の全体像、Web/GUIフラッシャの紹介）＋実習15分（各自 `sf flash` で標準ファームを書き込み） |
| 14:30-15:15 | 45分 | ③④ プログラム書き換え・モータの制御 | 講義・デモ10分（モータ配置/PWM/Duty/ARM/安全）＋実習30分（Duty書き換え→ビルド→書き込み→機体ボタンARMで回転確認、2〜3周）＋予備5分 |
| 15:15-15:30 | 15分 | まとめ | 振り返り・今後のロードマップ・Landingページ紹介・アンケート・質疑 |

**書き込みと設定（NVS）に関する注意:** 当日の書き込みは ② `sf flash`・③ `sf lesson flash` のどちらも設定保存領域（NVS）を消さないため、ペアリング情報と WiFi チャンネル設定は講座中ずっと保持される（②を Webフラッシャの full 書き込みから `sf flash` に変更したことによる。2026-07-16）。③はコントローラを使わず**機体本体ボタンのクリックでARM/DISARM**する設計（変更なし）。工場出荷相当への復元（full イメージの書き戻し。この時点でペアリング・チャンネルが消える）は講座終了後に講師が `setup-loaner-pc.md` §4 に従って行う。

> **【2026-07-18 訂正・更新】** 上記の「③でもチャンネル設定が保持される」は、旧 workshop ファーム（vehicle_old 基盤）ではチャンネルの**保存場所が標準ファームと別**（NVS `stampfly`/`wifi_ch` vs `sf_params`/`wifi.channel`）だったため**成立しておらず**、Day 1（7/18）に ch6/11 機でペアリング崩れとして顕在化した。同日夜に workshop ファームを現行 vehicle 基盤へ移行（`firmware/vehicle/docs/workshop_migration.md` §8）し、チャンネル・ペアリングの保存場所が標準ファームと**完全に共通化**されたため、以後この注意書きは構造的に成立する。移行後ファームは初回起動時に旧キーからチャンネルを自動取り込みする。**運用注意: チャンネル不一致状態の機体（旧 workshop ファームが残っている機体）に対しては再ペアリングを行わないこと**（コントローラ側が誤ったチャンネルにロックされるため）。

---

## 3. 各パートの詳細進行

### 3-1. オープニング（13:30-13:40、10分）

| 項目 | 内容 |
|------|------|
| 目的 | 講座全体の流れを共有し、安全ルールを周知した上で安心して機材に触れられる状態を作る |
| 使用機材・スライド | オープニングスライド（エコシステム紹介・本日の流れ・安全説明）、プロジェクタ |
| 講師の動き | 挨拶・自己紹介（航空宇宙工学→陸自で飛翔体の研究開発→高専→金沢工大ロボティクス学科。趣味=ロボット作り・読書・映画鑑賞）／StampFly・エコシステムの簡単な紹介／**現在地とビジョンを前向きに伝える**（StampFly は育ちざかりの教材＝位置制御まで飛ぶようになり、ここから授業パッケージへ仕上げていく段階。完成すると見えなくなる開発の生の姿＝コマンド・ビルド・調整の途中を体験できるのが今日の価値。初等〜高等・社会人教育まで見据えた教材群の完成を目指しその種まきに招く。**「提供できません」等の否定形・「時間をください」等の言い訳調は使わない — 進捗と価値を言い切る**）／本日4パートの流れを説明／安全ルール（保護メガネ着用・緊急停止＝DISARM操作の意味）を周知 |
| 参加者の動き | 着席、スライド視聴、保護メガネ受け取り |
| チェックポイント | 全員が保護メガネを装着し、緊急停止（DISARM）の意味を理解しているか |
| つまずき対応 | 遅刻者がいる場合は安全ルールだけ個別に補足してから合流させる |
| 時間超過時の短縮案 | 自己紹介・エコシステムの詳細紹介を省略し、安全説明とスケジュール共有のみに絞る（5分まで圧縮可） |

### 3-2. ① 操縦体験（シミュレータ→実機）（13:40-14:05、25分）

| 項目 | 内容 |
|------|------|
| 目的 | まずシミュレータで操縦感覚を掴み、その後 ALT_HOLD による実機の自動離陸→スティック操作→自動着陸を全員が最低1回体験する |
| 使用機材・スライド | 貸出PC（VPythonシミュレータ動作確認済み・コントローラは通信モード=USB HIDにプリセット）、事前ペアリング済み機体10セット、模様付きマット・ネット/区画で隔離した飛行エリア、操作手順スライド（シミュレータ手順・モード切替・LED色・操縦の図） |
| 講師の動き | 内訳は下表参照 |
| 参加者の動き | **シミュレータ**：コントローラをUSB接続→`sf sim run`→校正が終わるまでスティックに触らない→画面のStampFlyを操縦（スティック割当は実機と同じMode 3）。**切替**：M5ボタンでメニュー→「通信モード」を1回選択（USB HID→ESP-NOW、自動再起動）→USBを外す。**実機**：機体電源ON→緑常灯確認→左ボタン1回でALT_HOLD（LEDオレンジ）→右スティック押し込みでARM→自動離陸・高度自動維持→左スティックで移動（右は上下・旋回）→横に流れたら左スティックで戻す→右スティック押し込みでDISARM（自動着陸）。低電圧警告（シアン点滅+警告音）が出たら着陸してバッテリー交換 |
| チェックポイント | 全員がシミュレータと実機の両方で最低1回、離陸→操縦→着陸を体験できたか |
| つまずき対応 | シミュレータがコントローラを認識しない→USBを挿し直す・コントローラの通信モードがUSB HIDになっているか確認／切替後に実機が反応しない→通信モードがESP-NOWに戻っているか確認【リハーサル確認: メニューの正確なボタン操作】／機体が反応しない→コントローラの電源・ペアリング再確認／低電圧警告（シアン点滅+警告音）が出たら即座に予備機へ交換／横に流れるのは ALT_HOLD では正常（水平位置は自動保持しない）→スティックで戻すよう声かけ、流れが大きく壁に近づく場合は DISARM で着陸させ再スタート／緊急停止が必要な場面ではDISARMを2回（1回目=自動着陸開始、2回目=即時モータ停止） |
| 時間超過時の短縮案 | 押している場合はシミュレータ練習を5分に短縮する。実機は全員が最低1回の離着陸（ARM→ホバリング→DISARM）を完了していることを優先する |

**内訳（タイムライン）:**

| 時刻 | 内容 |
|---------|------|
| 13:40-13:42（2分） | 講師実演（`sf sim run` 起動→シミュレータ操縦→通信モード切替→実機ARMまでを一気に見せる） |
| 13:42-13:50（8分） | 各自シミュレータで操縦練習（コントローラはUSB接続のまま） |
| 13:50-13:52（2分） | 全員一斉にコントローラをESP-NOWへ切替（M5ボタン→「通信モード」1回→自動再起動）→USBを外し、機体の電源ON |
| 13:52-14:02（10分） | 実機飛行（ALT_HOLD・全員同時・交代なし）。低電圧警告が出たら着陸してバッテリー交換→再開 |
| 14:02-14:05（3分） | 全機着陸・電源OFF・バッテリー残量確認、総括、次パートへの導線 |

### 3-3. ② 開発環境+書き込み体験（14:05-14:30、25分）

| 項目 | 内容 |
|------|------|
| 目的 | コマンド（sf CLI）でファームウェアを機体に書き込む一連の流れを体験する。開発環境がない PC でもブラウザだけで書き込める仕組み（Landing ページ・Webフラッシャ・GUIフラッシャ）も紹介し、違いを理解する |
| 使用機材・スライド | sf CLI 全体像スライド、3方式比較スライド、GUIフラッシャ（StampFlyFlasher.exe）デモ（講師デモ機のみ）、各PCの開発環境（vehicle ファーム事前ビルド済み） |
| 講師の動き | デモ・紹介10分：(1) `install.bat` で開発環境一式（ESP-IDF + sf CLI）が一発導入される全体像を画面共有で紹介（実演はしない）、(2) 3方式比較スライドで Webフラッシャ「ブラウザだけで書き込める」を紹介（**参加者の実習はしない**。Landing ページの実演はまとめで行う）、(3) StampFlyFlasher.exe を講師機の「ローカルファイル」モードで実演。実習15分：各自 `sf flash vehicle` を実行、机間巡回で支援 |
| 参加者の動き | 機体電源ON→USB-C接続→コマンドプロンプトで `cd stampfly_ecosystem`→`setup_env.bat`→`sf flash vehicle`（PC内でビルド済みの標準ファームを書き込み）→完了後、機体が再起動し起動音が終わり緑常灯になるまで水平静置 |
| チェックポイント | 全員が `sf flash` での書き込みを完了し、機体が緑常灯に戻ったか。**`sf flash` は NVS（設定保存領域）を消さないため、ペアリング・WiFiチャンネルは保持されたまま** |
| つまずき対応 | 機体が認識されない→USBケーブル交換（充電専用ケーブルは不可）／別のUSBポートで再試行／書き込み失敗→ポートを使う他アプリ（シリアルモニタ等）を閉じる→機体のBOOTボタンを押しながらUSB挿し直して再実行 |
| 時間超過時の短縮案 | デモ・紹介を7分に圧縮し実習開始を早める。書き込みが終わらない参加者は③の冒頭で完了させる |

### 3-4. ③④ プログラム書き換え・モータの制御（14:30-15:15、45分）

| 項目 | 内容 |
|------|------|
| 目的 | モータ配置と Duty（PWMのオン時間比率）の関係を理解し、自分で書き換えたコードが実機のモータ回転として反映される体験を通じて「プログラム書き換え→ビルド→書き込み」の一連の流れを体感する |
| 使用機材・スライド | モータ配置図（FR/RR/RL/FL）・PWM/Duty説明スライド、ARM/安全説明スライド、各PCの開発環境（事前に `sf lesson switch dxh_workshop` / `sf lesson build` 実行済み） |
| 講師の動き | 内訳は下表参照 |
| 参加者の動き | `cd` でリポジトリへ移動→ `setup_env.bat` →`sf lesson edit`（VSCodeが開く）→コード内のDuty数値を書き換え→ `sf lesson build` →`sf lesson flash`（書き込み後シリアルモニタが自動起動、終了は `Ctrl+]`）→USBを外しバッテリー駆動に切替→起動LEDシーケンス確認→機体上部のM5StampS3を指で押さえた状態で本体ボタンをクリックしてARM→モータ回転を確認→もう一度クリックしてDISARM |
| チェックポイント | 全員が最低1回、指定したモータが指定Dutyで回転するのを確認できたか |
| つまずき対応 | ビルドエラー→`setup_env.bat` の再実行（`sf lesson switch` は当日再実行しない＝フルビルドになり時間内に収まらないため）／書き込み失敗→USBケーブル交換／モータが回らない→LEDが緑のゆっくり点滅（ARM状態）になっているか確認／Duty入力ミス（誤って `1.0f` 等と記入）→隣同士で数値をダブルチェックしてからビルドする運用を徹底 |
| 時間超過時の短縮案 | 2〜3周目（改造課題）を省略し、1周目を全員が確実に完了することを優先する。ビルド待ち時間が長い場合は2人1組のペア作業に切り替える |

**内訳（時間配分）:**

| 時刻 | 内容 |
|---------|------|
| 14:30-14:40（10分） | 講義・デモ：モータ配置（1=FR右前、2=RR右後、3=RL左後、4=FL左前）、Duty（0.0=停止〜1.0=全開、本講座では**0.15を上限とする安全運用**）、ARMの仕組み（コード内では自動ARMせず機体ボタンのクリックでARM/DISARM）を説明。講師が1台でデモ（Duty書き換え→ビルド→書き込み→ボタンARMで回転確認） |
| 14:40-15:10（30分） | 実習：各自コードを書き換え→ビルド→書き込み→回転確認、を2〜3周（Dutyを変える／別モータを回す等）。机間巡回で安全確認（必ず機体上部を指で押さえた状態で回す） |
| 15:10-15:15（5分） | 予備：早く終わった参加者への改造課題提示（4つのDutyに差をつけてARMし、ロール/ピッチ/ヨーの「傾こうとする力」を手で感じる。右>左=ロール、前>後=ピッチ、CW組>CCW組=ヨー。必ず基板を指で押さえたまま） |

**安全上の注意（講義で必ず伝える）:**

- Duty の上限はファームウェアでは強制されない（小数点のタイポで `1.0` と書くと全開で回る）。**隣同士で数値をダブルチェックしてからビルドする運用**を徹底する
- プロペラは装着したままで実施する。回転させる際は必ず機体上部の M5StampS3 基板を上から指で押さえた状態にしてから ARM する
- 動作確認は USB を外したバッテリー駆動で行う（USB接続中はARM自体が拒否される仕様のため、書き込み確認とモータ回転確認は工程が分かれる）

**実習コード（`docs/guides/motor_spin_quickstart.md` §6-3 準拠・正本）:**

`sf lesson edit` で開くと以下のコードが入っている（貸出PCに導入済み）。各モータごとに1行ずつ並んでおり、回したいモータの数値（Duty）を 0.0〜0.15 の間で設定すればそのモータが回り、0.0 のままならそのモータは止まったままである。

```cpp
#include "workshop_api.hpp"

// =========================================================================
// Motor duty test (hardcoded)
// 各モータの Duty をハードコードする
// =========================================================================
//
// Motor IDs / モータ ID:
//   1 = FR (右前)   2 = RR (右後)
//   3 = RL (左後)   4 = FL (左前)
//
// Duty range: 0.0 - 1.0
//   0.0  = stop / 止める
//   0.10 = spin at 10% / 10% で回す
//   このチュートリアルでは 0.15 を上限とする

void setup()
{
    ws::print("Motor duty test");

    // Do NOT auto-arm in code. Arming is controlled by the controller's
    // ARM button or a single click of the vehicle's onboard button, so
    // motors never spin until you intentionally press it.
    // コードでは自動 ARM しない。アーム/解除はコントローラの ARM ボタン
    // または機体本体ボタンの単クリックで行うので、意図的に押すまで
    // モータは絶対に回らない
}

void loop_400Hz(float dt)
{
    // ----- Set each motor's duty here / 各モータの Duty をここで設定 -----
    ws::motor_set_duty(1, 0.10f);   // FR (右前)
    ws::motor_set_duty(2, 0.00f);   // RR (右後)
    ws::motor_set_duty(3, 0.00f);   // RL (左後)
    ws::motor_set_duty(4, 0.00f);   // FL (左前)
    // -------------------------------------------------------------------
}
```

> 本講座の③④ではコントローラを使わず、機体本体ボタンの単クリックでARM/DISARMする（コード中コメントの通り、どちらの方法でもARMできる）。

### 3-5. まとめ（15:15-15:30、15分）

| 項目 | 内容 |
|------|------|
| 目的 | 本日の体験を授業活用のイメージにつなげ、フィードバックを得る |
| 使用機材・スライド | まとめスライド（今後のロードマップ2頁・Landingページ紹介・おわりに）、アンケートQRコード掲示（**2種類: 大学のDXH事業アンケート＋講師アンケート**。大学側アンケートの実施方法・URLは【要確認】） |
| 講師の動き | 本日の振り返り（3パートが操縦→書き込み→書き換えの段階構成だったことに触れる）／**現在地とビジョンを再度伝える**（位置制御まで到達し授業パッケージへ仕上げる段階・初等〜高等・社会人教育まで見据えた教材群を目指す・アンケートがその羅針盤）／今後のロードマップ（Tello互換 Python SDK・ブロックプログラミング）を紹介／Landing ページを実際にブラウザで開いて案内（帰ってからの入り口）／本日のスライド資料の公開先（ドクセル: https://www.docswell.com/s/Kouhei_Ito/ZL3EYL-2026-07-17-161742 ）を案内／アンケート2種類を案内（①大学のDXH事業アンケート=大学指定の方法で、②講師アンケート=Forms QRで。両方に回答いただく）／質疑応答対応 |
| 参加者の動き | アンケート回答（スマートフォン等）、質疑応答 |
| チェックポイント | 全員が2種類のアンケート（大学・講師）に回答した、または回答リンクを受け取ったか |
| つまずき対応 | アンケートフォームにアクセスできない→紙の予備を配布 |
| 時間超過時の短縮案 | 質疑応答を短縮し、アンケート回答時間を優先的に確保する |

---

## 4. 会場レイアウト

```
                          ┌───────────────────────────┐
                          │   プロジェクタ／スクリーン     │
                          └──────────────┬────────────┘
                                          │
  ┌──────────────────────────────────────────────────────────┐
  │              PC演習エリア（10席・2列×5・島型）                    │
  │   [PC01][PC02][PC03][PC04][PC05]                            │
  │   [PC06][PC07][PC08][PC09][PC10]                            │
  └──────────────────────────────────────────────────────────┘
   [講師席／操作卓]                                   [充電ステーション]
   (進行スライドPC・予備機・予備PC)                (予備バッテリー・充電器3〜4台)

  ┌──────────────────────────────────────────────────────────┐
  │       飛行エリア（ネット／フェンス／区画テープで隔離）              │
  │                                                              │
  │        ┌─────────────────────────────────┐                  │
  │        │   模様付きマット（光学フロー用）2〜3面     │                  │
  │        └─────────────────────────────────┘                  │
  │                                                              │
  │   [参加者の立ち位置 ×10（外周に等間隔・ネット越し）]                │
  └──────────────────────────────────────────────────────────┘
```

- PC演習エリアと飛行エリアは同一室内に隣接配置し、①→②③④の移動を最小化する
- 充電ステーションは講師席の近傍に置き、低電圧警告時の予備バッテリー交換を講師・アシスタントが即座に行えるようにする
- 参加者は飛行エリアの外周（ネット越し）に等間隔で立ち、各自の機体を自分の正面で飛ばす（動線・機体の交差を防ぐ）
- 会場（教室）の実寸・電源コンセントの位置・数は【リハーサル確認】。`equipment-checklist.md` の電源タップ本数（合計15〜16口以上）を参照して配置する

---

## 5. 当日朝のチェックリスト

詳細な機材チェックは `equipment-checklist.md` §3「当日朝チェックリスト」を正とする。以下は講師の進行準備に絞った時系列版。

| 目安時刻 | 項目 | 参照 |
|---------|------|------|
| 開始60分前 | 会場設営（PC10席＋講師席＋充電ステーションの配置、飛行エリアのネット／マット設置） | `equipment-checklist.md` §2 前日チェックリスト |
| 開始45分前 | 機体10セットの電源チェック（緑常灯までの起動確認）、事前ペアリングが崩れていないかコントローラLCDのMAC表示で再確認 | `setup-loaner-pc.md` §2-3 |
| 開始30分前 | PC10台の起動、`sf doctor` の抜き取り確認、講師デモ機の `C:\StampFly\`（GUIフラッシャ.exe・bin）確認 | `equipment-checklist.md` §3 |
| 開始15分前 | スライド投影・音声の確認 | — |
| 開始10分前 | アンケートQRコード掲示物（大学・講師の2種類）の設置、各PCでスライドPDFが開けることの抜き取り確認 | `equipment-checklist.md` §1 |
| 開始5分前 | 保護メガネの配布位置確認、緊急停止（DISARM）手順を講師・アシスタント間で再確認 | `docs/guides/safety.md` |

---

## 6. リスクと対応表

| リスク | 予兆・症状 | 対応 | 備考 |
|-------|-----------|------|------|
| 機体故障（モータ不動作・プロペラ破損） | 起動LEDシーケンスが進まない／異音／プロペラ破損 | 予備機に即交換。当該セットは講座終了後に原因調査 | 予備機2台を確保（`equipment-checklist.md`） |
| ビルドが遅い・固まる | ③④実習でビルドが実習時間内に終わらない | 隣席とペア作業に切替える | 事前フルビルド済みのため通常は差分ビルドで短時間の想定【リハーサル実測】 |
| Webフラッシャ書き込み失敗 | ポート未検出／進捗が進まない | GUIフラッシャの「ローカルファイル」モードに切替（オフラインでも動作） | `C:\StampFly\` に `StampFlyFlasher.exe` と `stampfly_vehicle_v2026.07.1_full.bin` を事前配置済み |
| クロスペアリング（取り違え） | コントローラが別セットの機体と繋がる | 事前ペアリング時（`setup-loaner-pc.md` §2-3）は候補一覧からMAC下4桁ラベルが一致する行を選ぶ運用を徹底し、機体LEDが緑常灯になったことで最終確認する。ペア済みのセットは崩さない運用を徹底し、セット番号ラベルとコントローラLCDのMAC表示で照合 | 機体は自分宛の電文だけを相手候補にするため、隣の組の電文だけでは取り違えは起こらない（`docs/plans/pairing-methods-plan.md`）。講座中は再ペアリング操作を一切行わない設計（②以降は機体ボタンでARM） |
| 低電圧で飛行時間内に電池切れ | シアン点滅＋警告音（3.4V） | 予備バッテリーに即交換 | 1セットあたり2本以上を用意（`equipment-checklist.md` §1） |
| 10台同時運用時の通信干渉 | 操縦が不安定／自動着陸が頻発 | コントローラのDevice ID割当とWiFiチャンネル分散（#01-04=1ch/#05-07=6ch/#08-10=11ch、setup-loaner-pc.md §2-2）を再確認。必要なら同時飛行数を絞る（数人ずつ順番に飛ばす運用に切替） | 干渉有無は【リハーサル実測】 |
| 教室ネットワーク制限（GitHub API等） | GUIフラッシャの「GitHub Release最新」モードが失敗・レート制限 | 当日は最初から「ローカルファイル」モードのみを使う運用に統一（Webフラッシャも同様に事前配置binへの依存はない点を確認） | `equipment-checklist.md` §1-8参照 |
| 時間超過 | 各パートが予定時間を超える | 3章の「時間超過時の短縮案」を該当パートに適用する | ③④（45分枠）が最も遅延しやすいため優先的に短縮対象とする |
| 参加者の操作不安・怪我懸念 | プロペラへの接触／機体真上を覗き込む 等 | 講師・アシスタントが常時机間巡回し、安全ルールをその都度声かけする | 保護メガネ着用の徹底、`docs/guides/safety.md` 準拠 |

---

## 7. アンケートと講座後作業

### アンケート

- 講座終了時（まとめパート内）にアンケートを実施する。回答は `survey/` に集約する（`survey/dxh_survey.md`（設問設計書）と `create_dxh_survey.gs`（フォーム自動生成スクリプト）を作成済み。script.google.com でスクリプトを実行しフォームURLを発行する作業が残っている）
- 本アンケートは高校教員からのフィードバック第1号にあたり、今後の高校向けカリキュラム改善の起点となる（`docs/plans/education-outreach-strategy.md` §7参照）

### 機材の復元

- 機材（機体・コントローラ）は講座終了後に工場出荷相当の状態へ復元する。復元手順は `setup-loaner-pc.md` §4「講座終了後の復元手順」を正とする（リリースFWへの書き戻し→再ペアリング→動作確認フライト→充電→数量確認）

### 改善メモの即日記録

- 講座終了当日中に、進行上気づいた改善点（時間配分・つまずき対応・会場レイアウト等）を本文書の該当箇所へ直接反映するか、次回セッション用のメモとして記録する
- リハーサルおよび1日目終了後の気づきは、2日目の進行までに本文書へ反映し、同一内容の講座であっても改善を1日目→2日目で回す
