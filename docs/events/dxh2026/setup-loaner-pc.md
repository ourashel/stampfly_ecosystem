# DXH講座 貸出PC・機材セットアップ手順書

> **DXH講座運営用の内部文書。** 高校教員には配布しない。講師・運営スタッフ専用。

## 0. この文書について

2026-07-18(土)・07-19(日) 開催の DXH（高等学校DX加速化推進事業）高校教員向け体験講座で使用する、大学貸出の Windows 11 PC 10台＋機材10セットの事前セットアップ手順とリハーサル計画をまとめる。準備締切は **7/17**。

対象読者はセットアップ作業を行う講師・運営スタッフ。手順は上から順に1台ずつ実施し、チェックボックスに沿って進める。

参照文書:

| 文書 | 内容 |
|------|------|
| `docs/setup/windows.md` | Windows 開発環境構築手順（本手順書のベース） |
| `docs/events/stampfly_workshop/workshop_guide.md` §機材チェックリスト | 通常勉強会の機材確認項目 |
| `docs/events/stampfly_workshop/workshop_schedule.md` §7 準備物 | 通常勉強会の準備物一覧 |
| `docs/events/dxh2026/equipment-checklist.md` | 本講座の機材数量・当日チェックリスト |

**表記ルール:** 未実測・未確認の項目には【リハーサル確認】【リハーサル実測】と明記している。断定できない数値・挙動をこの文書内で創作しないこと。リハーサルで判明した実測値は、本文書だけでなく `docs/events/dxh2026/handout.md` や `docs/events/dxh2026/curriculum.md`（別途作成予定）の該当箇所にも反映すること。

---

## 1. PC 1台あたりのセットアップ手順

貸出 PC 10台 + 予備1台の**合計11台**に対して、以下の手順を1台ずつ実施する。所要時間は【リハーサル実測】欄に記録する（§3参照）。

### 1-1. アカウント・権限の確認

- [ ] Windows へ管理者権限（または `winget` 実行に必要な権限）でログインできることを確認 【リハーサル確認】
- [ ] 大学の貸出PCがドメイン参加・グループポリシーで `winget` やインストーラの実行が制限されていないか確認 【リハーサル確認】
- [ ] 制限がある場合は情報システム部門に事前相談し、講座当日までに解除・代替手順を確定する

### 1-2. 前提ツールの導入（winget）

CMD を開き、以下を確認・導入する。

```cmd
git --version
python --version
```

- [ ] `git --version` が表示されない場合: `winget install Git.Git`
- [ ] `python --version` が Python 3.10 未満・3.13 以上・未導入の場合: `winget install Python.Python.3.12`（3.13以降は未対応。3.10〜3.12が必要）
- [ ] 導入後は CMD を開き直してから再確認する

### 1-3. リポジトリの取得

```cmd
git clone https://github.com/M5Fly-kanazawa/stampfly_ecosystem
cd stampfly_ecosystem
```

- [ ] `C:\stampfly_ecosystem` （またはチームで統一した場所）に clone できることを確認

### 1-4. install.bat の実行（ESP-IDF + sf CLI 自動導入）

```cmd
install.bat
```

- [ ] `install.bat` が ESP-IDF のダウンロード・インストールと sf CLI のセットアップを完了することを確認
- [ ] 所要時間を記録（§3のリハーサル記録シートへ）
- [ ] エラー時は `docs/setup/windows.md` §7 トラブルシューティング（Python 未検出時の PATH 設定等）に従う

### 1-5. VSCode のインストール

- [ ] VSCode 未導入の場合: `winget install Microsoft.VisualStudioCode`
- [ ] `sf lesson edit` がエディタとして VSCode を自動起動できることを後の手順（1-8）で確認する

### 1-6. 開発環境のアクティベートと動作確認

```cmd
cd stampfly_ecosystem
setup_env.bat
sf doctor
```

- [ ] `sf doctor` の全項目が OK になることを確認（NG があれば個別に解消してから先に進む）
- [ ] `sf sim run` で VPython シミュレータ（3D画面）が起動することを確認する（①冒頭で参加者が実行する。コントローラなしでも起動確認は可能、終了はウィンドウを閉じる/Ctrl+C）【リハーサル実測: 起動時間】

### 1-7. 機体の USB 接続確認

- [ ] StampFly を USB-C ケーブル（データ通信対応）で PC に接続する
- [ ] デバイスマネージャーの「ポート (COM & LPT)」に「USB JTAG/serial debug unit (COMx)」が表示されることを確認（ESP32-S3 内蔵USBのためドライバ不要のはず）【リハーサル確認】
- [ ] 認識されない場合はケーブルを充電専用でないものに交換し、別の USB ポートで再試行する

### 1-8. デスクトップフラッシャ類の配置

- [ ] `C:\StampFly\` フォルダを作成する
- [ ] `StampFlyFlasher_v2026.07.1_windows-x64.exe` を `C:\StampFly\` に配置する
- [ ] `stampfly_vehicle_v2026.07.1_full.bin` を `C:\StampFly\` に配置する（教室でGitHub Releaseの匿名APIレート制限（60回/時/IP）に当たらないよう、当日は「ローカルファイル」モードで使うため）
- [ ] `.exe` を1回起動し、SmartScreen の警告（未署名のため表示される）で「詳細情報」→「実行」を選び、正常に GUI が起動することを確認しておく（2回目以降は警告が出ない想定）【リハーサル確認】
- [ ] スライドPDF（リポジトリ内 `docs\workshop\slides\beamer\dxh_workshop.pdf`）のショートカットをデスクトップに作成し、ダブルクリックで開けることを確認する（**当日は配布資料を印刷せず、参加者はこのPDFを参照する**。2026-07-16 決定）
- [ ] 補足: ②の参加者実習は Webフラッシャから `sf flash` に変更（2026-07-16）。GUIフラッシャ .exe / full.bin は講師デモと講座終了後の復元（§4）用のため、必須配置は**講師デモ機のみ**（他PCへの配置は予備として維持してよい）

### 1-9. ファームの事前ビルド（vehicle + workshop）

当日は「書き込み」「編集→差分ビルド→書き込み」のみで進めるため、事前にフルビルドを済ませておく。

**②実習用の vehicle ファーム:**

```cmd
sf build vehicle
```

- [ ] `sf build vehicle` が成功することを確認する（②実習の `sf flash vehicle` はこのビルド成果物を書き込む）。所要時間を記録（§3）

**③実習用の workshop ファーム:**

```cmd
sf lesson switch dxh_workshop
sf lesson build
```

- [ ] `sf lesson switch dxh_workshop` で DXH 実習テンプレート（Lesson 90）に切り替える（従来の「Lesson 1 に switch → user_code_dxh.cpp を手動コピー」は不要になった。2026-07-17、テンプレートは `firmware/workshop/lessons/lesson_90_dxh_workshop/student.cpp` に移設）
- [ ] switch は**ビルドディレクトリを全削除しフルビルドになるため、当日この場では絶対に再実行しない**（当日は差分ビルドのみで時間内に収める）
- [ ] `user_code.cpp` の内容がDXH版（冒頭コメントに「DXH workshop exercise template」）であることを確認
- [ ] `sf lesson build` が成功することを確認し、所要時間を記録（§3）

### 1-10. 書き込みリハーサル（vehicle / workshop ファーム）

**② 実習の通し（vehicle）:**

```cmd
sf flash vehicle
```

- [ ] 書き込みが成功することを確認し、所要時間を記録（§3。②実習15分の見積り根拠）
- [ ] 書き込み後もペアリングが保持されている（コントローラと接続できる）ことを確認する（`sf flash` は NVS 非消去）

**③ 実習の通し（workshop）:**

```cmd
sf lesson flash
```

- [ ] 書き込みが成功し、書き込み後にシリアルモニタが自動起動することを確認（終了は `Ctrl+]`）
- [ ] 所要時間を記録（§3）
- [ ] `sf lesson flash` は NVS（設定保存領域）を消さないため、この時点ではペアリング情報は保持されたままであることを確認

### 1-11. リリースFWへの書き戻し（Webフラッシャ）

Lesson 1 ファームを書き込んだままにしないよう、講座当日の状態（操縦体験で使うリリース vehicle ファーム）に戻す。

- [ ] ブラウザ（Edge/Chrome）で https://m5fly-kanazawa.github.io/stampfly_ecosystem/flash/ を開く
- [ ] 「StampFly 本体」カードの「⚡ インストール」→ ポート選択 →「接続」→ INSTALL 実行（Erase device チェックは OFF）
- [ ] 完了後、機体が自動再起動し起動音が終わるまで水平静置することを確認
- [ ] **この書き戻しにより機体は未ペア状態に戻る**（full イメージはNVSも上書きするため）。ペアリングは §2 の手順でセットごとに行う

### 1-12. 完了チェック

- [ ] 上記 1-1 〜 1-11 全てにチェックが付いていることを確認
- [ ] PC本体・付属品（電源アダプタ等）に貸出セット番号ラベルを貼る（§2と対応）

---

## 2. 機材セットの準備（機体・コントローラ・PC・ケーブルの対応付け）

10セット（+予備）それぞれについて、機体・コントローラ・PC・ケーブルを1つのグループとして管理する。

### 2-1. セット番号ラベル

- [ ] 機体・コントローラ・PC・USB-Cケーブルの4点に、同一のセット番号ラベル（例: `#01`〜`#10`、予備は `#S1`）を貼付する
- [ ] ラベルは机上での取り違え防止のため、受講者が座る机にも同じ番号を掲示する

### 2-2. WiFiチャンネルの分散設定（ペアリングの前に・機体1台ずつ）

10台同時運用時の ESP-NOW 干渉を減らすため、機体の WiFi チャンネルを 2.4GHz 帯の非重複3チャンネル（1 / 6 / 11ch）に分散する（2026-07-16 決定）。チャンネルは**機体側の `wifi.channel` パラメータが正**で、コントローラはペアリング時に 1〜13ch をスキャンして機体が広告するチャンネルに自動でロックするため、コントローラ側のチャンネル操作は不要（そのかわり**設定変更後の再ペアリングが必須**）。

**チャンネル割当（4台／3台／3台）:**

| チャンネル | セット番号 | 台数 |
|-----------|-----------|------|
| 1ch | #01〜#04 | 4台 |
| 6ch | #05〜#07 | 3台 |
| 11ch | #08〜#10 | 3台 |
| 予備 #S1 | 交換先セットのチャンネルに当日合わせる（準備時は 1ch にしておく） | 1台 |

**設定手順（機体を USB 接続して1台ずつ）:**

```cmd
sf monitor
```

シリアルコンソール（機体のコマンド入力画面）で以下を入力する:

```
param set wifi.channel 6
param save
```

（`6` の部分はセットの割当チャンネル 1 / 6 / 11 に読み替える）

- [ ] `param save` 後、機体の電源を入れ直す（チャンネルは起動時にのみ読み込まれるため再起動で反映）
- [ ] 全10台＋予備に実施し、割当表どおりになっていることを確認する
- [ ] **この後の §2-3 ペアリングを必ず実施する**（コントローラはペアリングで新チャンネルにロックする。チャンネル変更前のペアリングは無効になる）

**注意:**

- 当日の書き込み（② `sf flash`・③ `sf lesson flash`）は NVS を消さないため、チャンネル設定とペアリングは講座中ずっと保持される
- 1日目終了後の復元（§4）でリリースFWを書き戻すとチャンネルも 1ch に戻る。**2日目の朝は本節の設定と再ペアリングを再実施**すること（最終返却時は既定 1ch のままでよい）

### 2-3. ペアリング（複数セット同時可・ラベル照合必須）

**以前は「1セットずつ、他は電源OFF」というルールだったが、現在は機体がペアリング中でも
自分宛（自 MAC 下3バイト一致）の電文だけを相手候補にする**ため
（`docs/plans/pairing-methods-plan.md` §4.1・実装は 79e2c7c8）、**複数セットを同時にペアリング
モードにしてよい**。ただし、コントローラの候補一覧には電波が届く範囲の他セットの機体も一緒に
表示される。これは選ぶ操作自体が人手であることに変わりはないため、**一覧からは必ず自分のセッ
トの機体（MAC下4桁ラベル）を選ぶこと**。他セットのラベルを誤って選ぶと、そのセットの機体との
間でペアリングが成立してしまう。

- [ ] 事前に全機体の MAC 下4桁ラベルを確認しておく（機体を USB 接続し `sf monitor` → `mac`
      コマンド）。既存のセット番号ラベル（`#01`等）に加えて、この下4桁も机・機体に掲示すると
      候補一覧との照合がしやすい

各セットについて（同時並行で進めてよい）:

- [ ] コントローラの M5 ボタン（画面下）を押しながら電源ON → ペアリングモード（`=== PAIRING ===`
      画面）に入る
- [ ] 機体の電源を ON にする（1-11 でリリースFWに書き戻し済み・未ペア状態）。本体のボタンを
      3 秒以上長押ししてビープで離す（LED が青の速い点滅になる）
- [ ] コントローラの画面に聞こえた機体が一覧表示されたら、**自分のセットの MAC 下4桁ラベルと
      一致する行**をスティック上下（または黄ボタン2つ）で選び、画面のボタンで確定する
      （候補が1件でもこの確定操作は必須）
- [ ] 確定後「waiting for vehicle reply...」に変わり、機体からの応答でペア成立することを確認する
- [ ] **機体の LED が緑常灯になったことを最終確認とする。** 万一ラベルを見間違えて別セットの
      機体を選んでしまった場合は、その機体の LED が緑になり、本来ペアリングしたかった機体は
      青点滅のまま残るので気づける
- [ ] コントローラLCDのMAC表示と機体のセット番号ラベルを照合し、意図した組み合わせになっていることを確認

### 2-4. コントローラの Device ID 割当

- [ ] コントローラのタッチメニューで Device ID を設定する。ID=0（親機）は1台のみに割り当て、他は 1〜9 の一意な値にする
- [ ] 10台同時運用時の干渉有無は【リハーサル実測】（§3）

### 2-5. スティックモードの統一

- [ ] 全コントローラのスティックモードが **Mode 3**（右スティック=スロットル/ヨー、左スティック=ピッチ/ロール）に統一されていることを確認する（講座は Mode 3 で教える。2026-07-16 決定）
- [ ] 確認方法: フライト画面の文字色が**黄 = Mode 3**（緑 = Mode 2）。違っていたらコントローラのメニューから Stick Mode を切り替える（設定は本体に保存され、電源を切っても保持される）

### 2-6. 通信モードのプリセット（USB HID・シミュレータ用）

①冒頭のシミュレータ体験（`sf sim run`）では、コントローラを USB 接続の HID デバイスとして使う（2026-07-17 決定）。ペアリング（§2-3）・Device ID（§2-4）・スティックモード（§2-5）を設定し終えた後に行う（通信モードの切替でこれらの設定・ペアリングは消えない）。

- [ ] 全コントローラの通信モードを **USB HID** にプリセットする（メニュー→「通信モード」を2回選択: ESP-NOW→UDP→USB HID。USB HID への切替時は自動で再起動する）
- [ ] 1セット以上で、USB接続した貸出PCの `sf sim run` がコントローラを認識する（起動時キャリブレーションが進む）ことを確認する【リハーサル確認】
- [ ] 当日は①の後半で参加者自身が ESP-NOW へ戻す（メニュー→「通信モード」を1回選択→自動再起動）。手順はスライドで案内する

### 2-7. バッテリー

- [ ] 各セットのバッテリーが満充電（4.2V）であることを確認する
- [ ] 予備バッテリーも満充電にしておく（必要本数は `equipment-checklist.md` 参照）

---

## 3. リハーサル手順と実測記録シート

準備締切（7/17）前に、少なくとも1セット・可能であれば2〜3セットを使ってフルリハーサルを行う。以下の表に実測値を記録し、確定した値を `handout.md` / `curriculum.md`（別途作成予定）の【リハーサル実測】箇所へ反映する。

| 測定項目 | 目標値 | 実測1 | 実測2 | メモ |
|---------|--------|-------|-------|------|
| 機体起動〜緑常灯（準備完了）までの時間 | 約10秒 | | | マゼンタ点滅（ジャイロ校正中）の長さも記録 |
| Webフラッシャでの書き込み時間（接続〜完了） | ― | | | ポート選択〜INSTALL完了まで |
| GUIフラッシャでの書き込み時間（SmartScreen込み） | ― | | | 1回目（SmartScreen通過あり）と2回目（警告なし）を分けて記録 |
| `sf lesson build` 差分ビルド時間（当日想定） | 実習15分に収まる時間 | | | 事前フルビルド後の再ビルドを想定 |
| `sf flash vehicle` 書き込み時間 | 実習15分に収まる時間 | | | ②実習の見積り。事前ビルド済み前提 |
| `sf lesson flash` 書き込み時間 | ― | | | モニタ自動起動までの体感時間も記録 |
| 操縦体験1サイクルの時間（ARM→ホバリング→操作→着陸） | 実習枠に収まる時間 | | | 全員同時飛行（交代なし）の25分枠内でのバッテリー交換頻度を見積もる |
| 10台同時飛行時の干渉有無（ESP-NOW/通信途絶） | 干渉なし | | | Device ID割当後の実測。通信途絶で自動着陸するため要観察 |
| `sf flash` / `sf lesson flash` 後にペアリングが保持されるか | 保持される | | | NVS 非消去の実機確認（②を `sf flash` に変更したため、当日 full 書き込みは行わない） |
| `sf sim run` 起動時間・コントローラHID認識 | 起動1分以内 | | | 通信モード=USB HID で起動時キャリブレーションが進むこと。メニューの正確なボタン操作も記録 |
| 通信モード切替（USB HID⇄ESP-NOW）でペアリング・設定が保持されるか | 保持される | | | 切替→再起動→実機接続までの所要時間も記録（①の切替2分枠の根拠） |
| Windows Defender による .exe / ビルドの阻害有無 | 阻害なし | | | SmartScreenとは別に、リアルタイム保護によるビルド速度低下や隔離の有無を確認 |

**記録後の対応:**

- [ ] 実測値が目標値と乖離する場合はカリキュラムの時間配分を見直す
- [ ] `docs/events/dxh2026/` 配下の当日資料（handout / curriculum、別途作成予定）の【リハーサル実測】箇所を実測値で置き換える
- [ ] 10台同時飛行に干渉が確認された場合は、同時飛行数を絞る運用（数人ずつ順番に飛ばす）や Device ID 割当を見直す

---

## 4. 講座終了後の復元手順

各日の講座終了後、翌日 or 返却に向けて機材を工場出荷相当の状態に戻す。

### 4-1. リリースFWへの書き戻し

- [ ] 各機体に `stampfly_vehicle_v2026.07.1_full.bin` を書き戻す。**GUIフラッシャのローカルファイルモードが確実**（Webフラッシャでも可）
- [ ] 各コントローラに対応するリリースFWがあれば同様に書き戻す（`stampfly_controller_v2026.07.1_full.bin`）

### 4-2. 再ペアリング

- [ ] 翌日も講座を行う場合は、先に §2-2 の WiFi チャンネル分散設定を再実施する（full 書き戻しで全機 1ch に戻っているため。最終返却時は 1ch のままでよい）
- [ ] §2-3 と同じ手順で再ペアリングする（複数セット同時でよいが、候補一覧からのラベル照合は必須）

### 4-3. 動作確認

- [ ] 各セットで ALT_HOLD による1フライト（自動離陸→ホバリング→自動着陸）を実施し、正常動作を確認する

### 4-4. バッテリー充電

- [ ] 全セットのバッテリーを充電する（充電中は目を離さない）

### 4-5. コントローラの通信モード確認

- [ ] 返却時は全コントローラの通信モードを ESP-NOW（既定）に戻す。翌日も講座がある場合は USB HID にプリセットし直す（§2-6）

### 4-6. 数量確認・返却準備

- [ ] `equipment-checklist.md` の機材一覧と照合し、数量に過不足がないか確認する
- [ ] 破損・不具合があった機材を記録し、返却前に大学側へ報告する
- [ ] セット番号ラベルを剥がす（返却物品に指示がある場合）
