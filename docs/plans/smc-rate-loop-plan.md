# レートループ スライディングモード制御(SMC)化計画 — `sf app`経由・SILS先行検証

作成: 2026-09-11

発端: 現行の`PidController`（カスケードPID、`firmware/vehicle/components/sf_controller_pid/`）を
スライディングモード制御(SMC)に置き換えたものを試したい。実機にいきなり書き込むのではなく、
**SILS（Software In the Loop Simulation）で検証できる形**で用意する。前回セッションで説明した
`sf app`コマンド（`IController`を差し替えて実機・SILS両方で動かす仕組み、`docs/plans/sf-app-sils-plan.md`
参照）がこの用途に合致するが、調査の結果`PidController::compute()`が632行のモノリシックな1関数で
高度・位置・離着陸フェーズが内部の私有状態と絡み合っており、丸ごとSMC化するのは大工事であることが
判明した。そこで置き換え範囲をレートループ3軸のみに絞り、既存`PidController`への委譲パターン
（`examples/11_app_controller`）を踏襲する設計とする。

ユーザー確認済みの方針（2026-09-11）:
- 置き換える範囲は**レートループ（`rate_roll_`/`rate_pitch_`/`rate_yaw_`）のみ**。角度外側ループ・
  高度・位置・離着陸フェーズ・誘導・トリム学習・ホバー推力学習・DOBは既存`PidController`に委譲。
- SMCのゲイン（スライディング面・到達則定数）は`sf_core/params.cpp`に新規行として追加し、
  `sf params`/NVS/`sf sils scenario --param`スイープをそのまま使えるようにする。

進捗（2026-09-11）: `firmware/apps/smc_rate`実装完了、`sf app sils`のWindows用CMakeバグ
（`simulator/sils/CMakeLists.txt`の`CONFIGURE_DEPENDS`付き`file(GLOB SF_APP_SRCS ...)`が
バックスラッシュパスを正しく扱えない）を副産物として発見・修正。§2.4の物理逆算初期ゲインは
`stab_flight`でFAILしたが、`sf sils scenario --target apps/smc_rate --param ...`による
再ビルド無しスイープでroll/pitch/yawとも到達則ゲインを強化・境界層を調整し、`params.cpp`の
デフォルト値として確定。最終ゲインで`alt_flight`/`acro_flight`/`stab_flight`/`yaw_hold`の
4シナリオが`--param`無しで一貫してPASS（`yaw_hold`の`alt_max`のみPID/SMC共通の既存無関係
問題でFAIL）。途中`smc.yaw.phi`を狭めすぎてstab_flightを壊す単一シナリオ過学習を実際に踏み、
広い方に戻して解消した実例あり（§3.1「yaw.phiのトレードオフ」）。超平面設計の考え方は
§2.2「超平面（スライディング面）の設計根拠」に記載。noise/摂動族ロバスト性確認（§3.2）で
**`stab_flight`×`torque-authority=0.55`にて明確な頑健性の弱点を発見**（PIDは耐えるが
SMCはatt_rmseゲート超過）——§3.1の詰めすぎによる単一点最適化の再発。実機投入は不可、
再チューニングが次の課題（§5）。

---

**2026-09-14、可読性改善のため全面整理した**（読み違えによる実機安全上の事故——§7.55続報が
まさにその実例——を防ぐため）。詳細な実測データ・全ログ解析は
[`smc-rate-loop-data.md`](smc-rate-loop-data.md)（旧§3/§7の生ログ、節番号は共通）へ分離し、
本ファイルは**結論・決定のみ**に圧縮した（5005行→約360行）。定量的な検証・数値の再確認が
必要な場合は、対応する節番号でデータ集を検索すること。読む前に次の2セクション
（現状サマリ・目次）に目を通すこと。

## 現状サマリ（既知の罠・確定済み結論 — 制御則やゲインを触る前に必ず確認）

本節は詳細な経緯を圧縮した「今すぐ知るべきこと」の一覧。各項目の詳細・根拠は
参照先の節番号（`grep -n "§7\.NN" docs/plans/smc-rate-loop-plan.md`で本文にジャンプ）を参照。

| # | 罠・結論 | 詳細 |
|---|---|---|
| 1 | **位置ループ`position.pos.kp`/`position.vel.kp`は触るな** — 既定(0.4/3.0)から動かす実機ゲイン変更は、2026-06-22に成長振動→壁激突を起こした`kp=1.0`と同じ危険域に踏み込みうる。`±6-7cm`はモータ/プロペラのトルク効き不足（実測0.4〜0.7倍）によるハード限界として既に確定済みの結論であり、ソフト側の伸びしろは無い | §7.55続報、`firmware/vehicle/docs/poshold_journey.md`§4 |
| 2 | **SILSの既定条件（`--noise off`・トルク効き1.0）は理想プラント** — この条件だけで「改善」と判定した変更が実機で悪化する実例が既に2件ある（2026-06-22のPOS_HOLD発散、2026-09-14のkp=1.0再現）。制御則の実機投入判断は必ず`--noise`と劣化注入（`--motor-delay`/`--thrust-eff`/`--torque-authority`等）込みで検証すること | §7.55続報、poshold_journey.md§4.3 |
| 3 | **「ゲインを上げる/下げるだけ」は繰り返し非単調に反証されている** — §3.1（yaw.phi単一シナリオ過学習）、§7.31続報2〜4、§3.7〜3.8、§7.55（位置ループ共振トラップ）など多数の実例。粗いスキャン1回の「改善」を根拠に提案しない（CLAUDE.md規定） | §3.1, §7.31系列, §7.55 |
| 4 | **実機での発散を「根本解決」できたのは、ゲイン探索でなく遅延/権限不足そのものへの対処だった** — レート/速度ループの実機発散は**Smith予測器によるむだ時間補償**（§7.54続報10）で解決、位置ループの実機発散は**カスケード時間スケール分離の回復**（vel.kp↑・pos.kp↓, 2026-06-22）で解決——後者は真因（モータのトルク不足）自体は未解消のband-aid | §7.54続報7/10, poshold_journey.md§4.4 |
| 5 | **実機投入状況（コントローラ別、2026-09-14時点）**: `vehicle`(PID)=主力・POS_HOLD±6-7cm実証済み／`smc_rate_sta`(固定STA・レートのみ)=実機投入済み（§7.16、z漏れ積分込み）／`smc_rate_asta`(適応STA・レートのみ)=`duty_max`受容はしたがユーザーの実機投入承認は未実施／`smc_pos`・`smc_pos_sta`(位置までSMC/STA)=実機クラッシュ既往（§7.28）につきSILS限定方針／`smc_pos_asta`(適応STA・位置まで・Smith予測器込み)=**実機に複数回投入・飛行済み**（§7.52〜7.54続報13） | §7.16, §7.28, §7.40/7.41, §7.52-7.54続報13 |
| 6 | **既知のドキュメント不整合**: `firmware/apps/smc_pos_asta/README.md`は「実機投入は行わない」という初期方針のまま更新されていない — 実際には上記5の通り複数回実機投入・飛行済み（Smith予測器の実測パラメータを設定した上で）。当該READMEを読む際は本計画書の§7.5x系列を優先し、古い記述を鵜呑みにしないこと | §7.52-7.54続報13 |

## 目次

- **§0** 要旨 / **§1** 前提・制約 / **§2** 設計判断（2.1スコープ・2.2制御則・2.3コード構成・2.4パラメータ追加・2.5アーキ整合）
- **§3** SILS検証手順（初回チューニング〜実機クラッシュログ解析、3.1〜3.15）
- **§7** 位置制御ロバスト化 — 本編（詳細データは[`smc-rate-loop-data.md`](smc-rate-loop-data.md)参照）。時系列の大きな流れ:
  - **7.1〜7.9** 速度ループSMC化 第1弾（`smc_pos`） → 7.9で「見送り」判断
  - **7.10〜7.24** STA導入・`smc_rate_sta`実機投入・持続外乱での転倒発見・z漏れ積分安全機構
  - **7.25〜7.30(+続報1-8)** `smc_pos_sta`位置制御 再挑戦 → 実機クラッシュ（7.27/7.28）→ 持続振動の原因究明（ESKF accel-compまで遡る診断、7.30続報1-8）
  - **7.31〜7.49(+続報)** `smc_rate_asta`適応STA — 多数のチューニングサイクル（7.31続報1-4非単調性、7.32-7.36収束、7.37高度暴走、7.38-7.41 duty限界の受容、7.44-7.49離陸失敗の真因確定）
  - **7.50〜7.54(+続報1-13)** `smc_pos_asta`（適応STA・位置まで） — チューニング(7.51)→実機投入(7.52)→事故解析(7.53)→レートループ再同定(7.54)→Smith予測器実装(7.54続報10)→実機投入・実機テレメトリ確認(7.54続報12-13)
  - **7.55(+続報)** 位置ループkp探索 → **実機危険値と判明し撤回**（現状サマリ#1参照）
  - **7.56(+続報1-6・完結)** 「第3のループ」（§7.30）を`smc_pos_asta`向けに再検証 → Smith予測器`tau_m`延長では解決せず、`k1`の自己ラチェットが機序と特定。`dead_band`/`leak_ratio`/`adapt_rate`/`mref_trend_floor`/`k1_max`——主要パラメータ全てを（ノイズ有効化込みで）探索したが、全ノイズ条件で一貫して既定を上回る設定は見つからず。`k1_max`を上げても`k1`は無制限に成長せず自然な均衡点に収まる（安全上の安心材料）が、改善はノイズ条件依存で汎化しない。**パラメータ探索は完全打ち切り**——構造再設計以外に道はないという結論で決着
  - **7.57(+続報・完結)** 適応則の構造再設計（比例成長/漏洩・エネルギー積分発散判定）→ どちらも改善せず。続報で`filter_tau`（判定前のLPF強化、ノイズ低減アプローチ）も検証——単軸機動では改善したが本来の標的（長時間保持）では`noise=n0`で悪化し不採用。`dead_band`/`leak_ratio`/`adapt_rate`/`mref_trend_floor`/`k1_max`/`prop_adapt_gain`/`energy_trend_floor`/`filter_tau`——**この適応則のほぼ全レバーを探索し尽くし、解決策なしという結論で「第3のループ」調査全体（§7.30・§7.56・§7.57）は完結**
- **§4** 実機投入ゲート / **§5** 進捗管理（やったこと／やっていないこと — 全節横断の一覧表、散在する「今後の方針」チェックリストの集約先）/ **§6** 参考文献

## 0. 要旨

| 観点 | 内容 |
|------|------|
| 現状 | 唯一の`IController`実装は`PidController`。`control_task.cpp:189`が`sf::app::controller()`経由で取得し、`SF_APP_DIR`未指定時は`app_default.cpp`→`stock_hooks.cpp`の関数ローカルstatic`PidController`に解決される |
| 制約 | `PidController::compute()`（`pid_controller.cpp:214-846`）は姿勢・高度・位置・離着陸フェーズ・誘導・トリム学習・DOBが1関数に同居するモノリシック実装。個別ループを外部から呼び分ける公開APIは無い |
| 突破口 | `ControlOutput`は`rate_ref[3]`（最終レート目標、姿勢外側ループ・ヘディングホールド・誘導など全適用後、`pid_controller.cpp:824-826`で書き込み）を既にテレメトリ用に公開している。`pid_.compute()`を丸ごと呼び、その`torque[0..2]`だけをSMCで再計算した値に差し替えれば、他は無改造で委譲できる |
| 方針 | `sf app new smc_rate --from 11_app_controller`で生成したappプロジェクト内に、`PidController`をメンバとして保持しつつ`compute()`の`torque[0..2]`だけを1次スライディング面+境界層付き到達則で上書きする`AppController`を実装する |
| 成果目標 | `sf app sils smc_rate`で既存のACRO/姿勢系シナリオ（`.expect`の`att_rmse`/`duty_max`等）をそのまま使い、`sf sils scenario`（PIDベースライン）と直接A/B比較できる状態 |
| 実機投入 | SILS摂動族テスト（後述）をクリアし、ユーザーの明示的な判断を得てから`sf app build/flash smc_rate`に進む。development_roadmap.mdの層別検証（ACRO起点）を踏襲 |

## 1. 前提・制約（調査で判明した事実）

| 項目 | 内容 | 根拠 |
|------|------|------|
| `IController`インターフェース | `compute`/`reset`/`onModeChange`が必須。`onLanding`/`onTakeoff`/`onTakeoffComplete`/`isTakeoffComplete`/`setGuidanceTarget`/`isGuidanceActive`/`startExcitation`/`fetchSysidResult`/`reloadParams`は既定no-op | `firmware/vehicle/components/sf_controller/include/controller.hpp` |
| `ControlOutput`の中間目標公開 | `rate_ref[3]`（内側ループ角速度目標[rad/s] R,P,Y）・`angle_ref[2]`（外側ループ傾き目標[rad] R,P）がData Stream用に公開済み | `firmware/vehicle/components/sf_core/include/data_types.hpp:236-242` |
| 上記が実際に書き込まれる箇所 | `output.rate_ref[0..2] = rate_sp_roll/pitch/yaw`（ヘディングホールド・誘導・POS_HOLD・Landing水平ゲート適用後の最終値） | `firmware/vehicle/components/sf_controller_pid/pid_controller.cpp:824-826` |
| レートPID最終呼び出し | `rate_roll_.compute(rate_sp_roll, gyro_rate.x, dt)`等 — 我々のSMCが置き換える対象そのもの | `pid_controller.cpp:774-776` |
| 実行タイミング | `ControlTask`（優先度23、IMU同期400Hz、`ImuTask`からの通知で起床）が`controller.compute(state, setpoint, config::IMU_DT)`を呼ぶ。dt=2.5ms固定 | `firmware/vehicle/tasks/control_task.cpp:189, 233-241, 360` |
| 出力の下流 | `sf::control_output.publish(control)`後、`Actuator::update()`が独立に`control_output.latest()`を読みB⁻¹ミキサー→モータ曲線→duty clampへ。両者は疎結合（トピック経由） | `control_task.cpp:376, 407`、`firmware/vehicle/components/sf_actuator/actuator.cpp:255-309, 352-370` |
| ミキサー（B⁻¹） | `T_i = ¼T ± τφ/d ± τθ/d ± τψ/κ`（d=ARM_D=0.023m、κ=KAPPA=4.10e-3=Cq/Ct）。線形幾何配分。SMCのトルク出力もこの形式（物理単位, torque[Nm]+thrust[N]）で受け取られる | `actuator.cpp:88-134, 255-273` |
| 既存トルク上限 | `max_roll_pitch_torque_=5.2e-3 Nm`（param `rate.*.output_limit`相当）、`rate.yaw.max_torque`（既定1.226e-3 Nm、実行時調整可） | `pid_controller.hpp:594-595`、`params.cpp`該当行 |
| 物理パラメータ | mass=0.037kg, Ixx=9.16e-6, Iyy=13.3e-6, Izz=20.4e-6 kg·m²（SSOT） | `control/models/stampfly_physical.yaml:63-108` |
| モータ曲線の不確かさ | SILSプラント用`measured_2026_07`と実機`flight_anchored_motor_curve`は約1.252倍のホバー推力差があり未解決（`simulation-policy.md`§8 backlog #3） | `control/models/stampfly_physical.yaml:372-417` |
| パラメータストア | `sf_core/params.cpp`の`param_vars`+`table[] `が唯一の実行時チューニング機構（`{name, type, &var, default, min, max, callback}`）。`set_float`→`notifyControllerReload`→`ControllerCmd::ReloadParams`→`IController::reloadParams()` | `firmware/vehicle/components/sf_core/params.cpp:707+`、`control_task.cpp:157-163` |
| SILSビルド対象 | `sf app sils <name>`は`emu_vehicle`（host-native, MinGW+MuJoCo）を`SF_APP_DIR`付きでビルドし、`.scn`シナリオ+`.expect`（`log_contains`/`order`/`exit`/`metric ... in t0 t1`）で自動判定 | `lib/sfcli/commands/sils.py`（`build_app_emulator`/`run_scenario_with_exe`/`_eval_expect`） |
| 既存シナリオ資産 | `simulator/sils/scenarios/`に79ファイル（`.scn`+`.expect`）、`TEST_MATRIX.md`にカテゴリ一覧。ホバー/姿勢/高度/位置/wobble_bench/gain-deficit等 | `simulator/sils/scenarios/TEST_MATRIX.md` |
| 頑健性検証の既存原則 | 単一点最適化禁止（`torque-gain∈[0.4,0.7]`・`dead-time L∈[8,15]ms`等の摂動族で検証必須）。過去に公称SILSだけで最適化したゲインが実機で位相余裕-375°になった実例あり | `docs/architecture/simulation-policy.md`§6 |
| INV不変条件 | INV-1（全飛行フェーズが単一姿勢+レートパイプラインを共有）、INV-2（パイロットの姿勢権限）、INV-5（ミキサー入力は物理単位） | `firmware/vehicle/docs/architecture.md`「アーキテクチャ不変条件」節 |
| コーディング規約 | 1関数50行以内・バイリンガルコメント・マジックナンバー禁止・`@design`タグ必須（`[OK]`/`[NG]`/`[--]`） | `firmware/vehicle/docs/coding_and_education.md` |
| プロジェクト横断ルール | 制御系パラメータの変更提案は数値シミュレーションで裏付けてから行う（推測での提案禁止） | `CLAUDE.md`セッションルール |

**既存の非PID/非線形制御の実装例**: リポジトリ内にスライディングモード・Lyapunov・バックステッピング等の先行実装は無く、本計画は純粋な新規実装（greenfield）。`IController`のdocstring自体が「PID, MPC, LQR, or any custom controller」の差し替えを想定している。

## 2. 設計判断

### 2.1 スコープ

| 案 | 判断 |
|----|------|
| レートループのみ置き換え | **採用**（ユーザー確認済み）。`rate_ref`が既に公開されており最小侵襲。既存`rate_sysid.py`による同定と直接比較可能 |
| 姿勢+レート結合（単一スライディング面） | 不採用（今回）。角度ループも壊すためカスケード構造の比較優位性が失われる。将来の拡張候補として保留 |
| フルスクラッチ（高度/位置/離着陸も自前実装） | 不採用。`PidController::compute()`の私有状態を丸ごと再実装する必要があり、リスク・工数に見合わない |

### 2.2 制御則

各軸（roll/pitch/yaw）を独立3系統として設計する（`ω×Iω`ジャイロ交差結合は無視 — 既存`rate_sysid.py`の軸別同定も同じ簡略化を前提としており整合）。

1次スライディング面（測定レート誤差そのもの）:
```
s = ω_ref − ω          ω_ref = baseline.rate_ref[axis]（PidController由来）、ω = state.angular_rate[axis]
```

境界層付き到達則（チャタリング抑制のため`sign()`の代わりに`sat()`）:
```
τ_axis = I_axis · ( k·sat(s/φ) + η·s )
```
この形は「定数項＋比例項」型到達則（constant-plus-proportional rate reaching law、
通称exponential reaching law）— Gao & Hung [R1]が体系化した設計法に基づく。境界層による
チャタリング抑制（`sign()`→`sat()`の置換）はSlotine & Li [R2]の標準的手法。文献一覧は
§6参照。

| 記号 | 意味 | 備考 |
|------|------|------|
| `k` | スイッチングゲイン | 外乱・モデル不確かさの上限を上回る必要（SMCの頑健性の核） |
| `η` | 線形到達ゲイン | 境界層内の収束速度 |
| `φ` | 境界層半幅 | チャタリングと追従誤差のトレードオフ |

出力は既存PIDと同じ上限（`max_roll_pitch_torque_`/`rate.yaw.max_torque`、§1参照）でハードクランプし、物理的なトルク上限をPIDと共有する。

#### 超平面（スライディング面）の設計根拠 — なぜ`s = e`で足りるか

一般にスライディング面設計は`s = ė + λe`のような複数項の線形結合（面上のダイナミクスが
Hurwitz安定になるよう`λ`を選ぶ、極配置に相当）が必要になる。これが要るのは**制御入力から
見た出力の相対次数が2以上**の場合 — 例えば角度`θ`を直接追従させる系（`τ→(積分)→ω→(積分)→θ`、
トルクが角度に効くまで積分2回＝相対次数2）では、角度誤差`e_θ`だけを面にすると`ṡ`が`τ`に
依存せず到達則が組めないため、`s = ė_θ + λe_θ`として角速度誤差を混ぜ込み`ṡ`に`τ`（`ω̇`経由）を
出現させる必要がある。この`λ`が面上の収束時定数`1/λ`を指定する設計パラメータになる。

本計画は§2.1のスコープ決定により**レートループのみ**を対象とし、追従対象は角速度`ω`そのもの
（`τ→(積分1回)→ω`、剛体オイラー方程式`I·ω̇=τ`、`ω×Iω`交差結合は無視 — 相対次数1）。よって
`s = ω_ref − ω`のままで`ṡ = ω̇_ref − τ/I`が既に`τ`に直接依存し、複数項への結合は不要かつ
（状態が1つしかないため）結合のしようがない。**これは設計を省略したのではなく、相対次数1の
系に対する教科書通りの最小構成**であり、本計画で実際に「設計」したのは超平面の形状ではなく
到達則側の3定数（`k`/`η`/`φ`）のみである。§2.1で「姿勢+レート結合」案を不採用としたのは、
まさにこの`s = ω_error + λ·θ_error`という2項の面・`λ`という設計パラメータの発生を避けるため
でもある（採用していた場合は極配置または`stab_flight`許容オーバーシュートからの逆算が必要
だった）。

**明記すべき簡略化**（実装コメント・`@design`タグで残す）:
- `ω̇_ref`のフィードフォワード項は初版で省略（400Hz更新の`rate_ref`を数値微分すると量子化ノイズが乗るため）
- モータの電気/機械遅れ（`rate_sysid.py`が同定する時定数`T`）はトルク指令→実レート応答間の未モデル化の速い動特性。到達ゲイン`k`を上げすぎるとこれを励振しチャタリング・振動を招くため、初期ゲインは保守的に設定しSILS摂動族テストで確認する

### 2.3 コード構成

```
firmware/apps/smc_rate/              ← `sf app new smc_rate --from 11_app_controller`
├── app.yaml                         (type: embedded, sils: true)
├── app_controller.hpp/.cpp          ← AppController書き換え
└── smc_rate.hpp                     ← 新規: SlidingModeRate struct（pid.hppのPID structと同形）
```

`smc_rate.hpp`（`firmware/vehicle/components/sf_controller_pid/include/pid.hpp`の`PID` structと同じ形— ゲイン+状態+`compute()`+`reset()`）:
```cpp
struct SlidingModeRate {
    float k = 0, eta = 0, phi = 0.05f;   // ゲイン（reloadParams()でparams.cppから読む）
    float output_limit = 1.0f;            // 既存の max_roll_pitch_torque_ / rate.yaw.max_torque を流用
    float compute(float rate_sp, float rate_meas, float dt);
    void reset() {}   // 積分状態を持たないためno-op（将来拡張用に形だけ残す）
};
```

`app_controller.cpp`の`compute()`フロー:
```cpp
sf::ControlOutput AppController::compute(const sf::StateEstimate& state,
                                          const sf::CommandSetpoint& setpoint, float dt)
{
    sf::ControlOutput output = pid_.compute(state, setpoint, dt);   // 高度/位置/離着陸/誘導/トリム/DOBは丸ごと既存ロジック
    output.torque[0] = smc_roll_.compute(output.rate_ref[0], state.angular_rate[0], dt);
    output.torque[1] = smc_pitch_.compute(output.rate_ref[1], state.angular_rate[1], dt);
    output.torque[2] = smc_yaw_.compute(output.rate_ref[2], state.angular_rate[2], dt);
    return output;   // thrust/rate_ref/angle_refはpid_の値を維持（比較プロット用に目標値は据え置き）
}
```

`reset()`/`onModeChange()`/`onLanding()`/`onTakeoff()`/`onTakeoffComplete()`/`isTakeoffComplete()`/
`setGuidanceTarget()`/`isGuidanceActive()`/`startExcitation()`/`fetchSysidResult()`は`pid_`へ単純転送
（`11_app_controller`の`AppController`と同じパターン）。`reloadParams()`は`pid_.reloadParams()`に加え、
`smc_{roll,pitch,yaw}_`の`k/eta/phi/output_limit`もparamsから再読込するよう拡張する。

**副次的な利点**: `startExcitation`（sysid用チャープ/ダブレット注入）は`PidController`内部で`rate_sp_*`に
加算されてから`output.rate_ref`として公開されるため、`pid_`へ転送するだけでSMCも既存の
`sf sils sysid-gate`/`rate_sysid.py`の励振経路をそのまま受けられる（追加実装不要）。

### 2.4 `sf_core/params.cpp`への追加

`rate.roll.kp`等と並べて`smc.{roll,pitch,yaw}.{k,eta,phi}`（計9行）を`param_vars`+`table[]`に追加。
コールバックは既存の`notifyControllerReload`を流用。初期値・min/max境界は、既存レートPIDゲイン
（`rate.{roll,pitch,yaw}.kp/ti/td`）と`I_axis`から到達則が同等の閉ループ帯域になるよう逆算した値を
**SILS実装時に確定する**（ここで数値を仮置きしない — CLAUDE.mdの「制御系パラメータはシミュレーション
で裏付けてから」原則に従う）。これは標準vehicleの挙動を一切変えない追加のみの変更（デフォルト
ビルドでは未使用の追加パラメータ行が増えるだけ）。

### 2.5 アーキテクチャ不変条件との整合

| INV | 整合性 | 理由 |
|-----|--------|------|
| INV-1（全飛行フェーズが単一パイプラインを共有） | 自動的に満たす | 鉛直フェーズ分岐は`pid_.compute()`内部にそのまま残る。本変更は姿勢レートの最終段のみ |
| INV-2（パイロットの姿勢権限） | 自動的に満たす | 角度→レート目標の生成は無改造。SMCは追従則が変わるのみ |
| INV-5（ミキサーは物理単位） | 満たす | `torque[Nm]`をそのまま出力、ミキサー入力形式は不変 |

`@design`タグを`app_controller.cpp`の`compute()`に付与し、`architecture.md`のIController差替え節・
上記INV番号を参照。ステータスは実装直後`[--]`→SILS検証後`[OK]`とする。

## 3. SILS検証手順

> **詳細データ（全表・全ログ解析）は[`smc-rate-loop-data.md`](smc-rate-loop-data.md) §3.1-3.15に移動した。**
> 以下は各節の結論のみ。

`smc_rate`（1次SMC、レートループのみ）の初回チューニング〜実機クラッシュログ解析までの記録。

- **3.1** 初回チューニング結果（2026-09-11, noise=off）。yaw軸チューニングで`yaw.phi`を狭めすぎ`stab_flight`を壊す単一シナリオ過学習を実際に踏み、広い方に戻して解消（以降繰り返し現れる「過学習」パターンの初出）
- **3.2** ロバスト性確認（noise/摂動族）で`stab_flight`×`torque-authority=0.55`に明確な弱点を発見（PIDは耐えるがSMCはatt_rmseゲート超過）。`--noise n2`でPIDだけ離陸未達という別問題も発見（原因は§7.42で特定）
- **3.3** 頑健性弱点への対処を試行（8点探索）。`torque-authority`は`eta`増で解決可能と判明したが副作用で`motor-delay=15ms`が転倒——安全性を優先しラウンド1へ差し戻し
- **3.4-3.5** PI型スライディング面（積分項付加）を提案・実装。`torque-authority`のatt_rmseを約30%改善したが3.0°ゲートには未達
- **3.6** 積分アンチワインドアップを追加（レビュー起点）
- **3.7** `η`/`λ_i`同時再探索で`torque-authority=0.4`が本計画で初PASS
- **3.8** スイッチングゲイン`k`再探索で`torque-authority=0.55`が初PASS、しかし他条件が逆転——非単調トレードオフの初出
- **3.9** ACRO限定で実機書き込み（2026-09-11）
- **3.10** POS_HOLD位置制御崩れの実機報告→SILSで原因特定（§7.30系列へ継続）
- **3.11** 実機トリム仮説・noise仮説をいずれも否定
- **3.12** `sf log capture`が現行ファームで機能しない既存ギャップを発見（SMC無関係）
- **3.13** POS_HOLD系シナリオ全4本のカバレッジ完了
- **3.14**【重要・運用注意】WiFi STAモード切替でESP-NOW（プロポ通信）が切れる事故——運用手順に反映
- **3.15** 実機クラッシュのログ解析。モータ非対称性を実測——この発見が§7の位置制御ロバスト化に着手する発端になった

## 7. 位置制御ロバスト化（速度ループSMC化 → 適応STA → Smith予測器、`firmware/apps/smc_pos*`/`smc_rate*`）

> **詳細データ（全SILSゲート数値表・実機テレメトリ解析表・ゲイン掃引表・文献リスト）は
> [`smc-rate-loop-data.md`](smc-rate-loop-data.md) §7.1-7.55に移動した。以下は各節の
> 決定・結論のみを圧縮した記録——数値の再確認が必要な場合は必ずデータ集の該当節番号を参照すること。**

### アーク1（§7.1-7.9）速度ループSMC化 第1弾（`smc_pos`）→ 見送り

`smc_pos`（`smc_rate`のレートSMC + 新規速度SMC）を新設し複数ラウンド検証したが、最終的に**位置制御へのSMC投入を見送り**（ユーザー判断、2026-09-11）。

- **7.1** 着手の経緯。`firmware/vehicle/docs/poshold_journey.md`§4を確認し、既存PIDが実機同定に基づき既に頑健化・実機検証済み（±6-7cm）と判明——「既存が壊れているから」でなく既存PIDとのA/B/C比較として実装方針を確定
- **7.2** ラウンド1 SILS結果（未チューニングseed）。att_rmseは3者中最良もduty_maxがPID同様1.0飽和
- **7.4** ラウンド2でduty飽和を解消（k/eta/phi 4点スイープ）、フルバッテリー検証は良好だが**motor-delay=15msでdrift=4.36の明確な発散**
- **7.5-7.6** motor-delay発散への対処を2方向試すが両方悪化——単純なゲイン調整では解決不能と判明
- **7.7-7.7e** 無駄時間予測補償器（Smith予測器の積分器プラント特化・簡略版）を試行。motor-delay=15msには劇的効果があったが、**実機の軸別遅れ実測値の誤り（pitch 8.4ms→正しくは17.3ms）を訂正した結果、「安全」と判断していた設定が壊滅的に発散すると判明**（drift 0.51→23.12）。安全マージンが極めて狭く実機投入は不適と結論
- **7.9**【結論】速度ループSMC自体は安全だが既存PIDを明確に上回らず、無駄時間予測補償器も実用マージンを持たない——**「安全」×「明確に優れている」の両方を満たせず、位置制御SMC化はこれ以上進めない**。`smc_rate`（レートのみ）単体はこの結論と独立

### アーク2（§7.10-7.24）STA導入・`smc_rate_sta`実機投入・持続外乱での転倒発見・安全機構

- **7.10** スライディング面の不感バンドを試行——効果なし（`stab_flight+motor-delay=15ms`のtilt_maxはほぼ不変）
- **7.11-7.12** スーパーツイスティング法（STA、2次SMC）を新規`smc_rate_sta`として実装。k1/k2スイープでk1=140/k2=70を採用
- **7.13**【重要】`stab_flight`単体では良好だったk1=140/k2=70が、`pos_flight`（複合機動）+motor-delay=15msで壊滅的破綻（tilt=35.92°）——**単一シナリオ検証の危険性を実証、実機投入を中止**
- **7.14** 3アーキタイプ（stab_flight/pos_flight/新設`stab_combined_aggressive`）同時ゲートで再チューニング、**k1=60/k2=30を採用**——以降のSTA系の基準ゲインになる
- **7.16** `smc_rate_sta`（k1=60/k2=30）を実機投入（2026-09-11、まだ飛行なし）
- **7.17**【重要・実機投入後に発覚】持続外乱（60秒ロール保持+torque-authority=0.4）でSTA積分器が周期的リセットを繰り返しながら悪化、**約33秒で転倒**——実機飛行を保留
- **7.18** 安全機構「zの漏れ積分」（leaky integration）を追加（既定`z_leak_tau=1.0s`）。転倒解消・既存6条件で回帰なしを確認、実機へ再投入
- **7.19** z漏れ積分入り初の実機飛行（2026-09-11）——tumbleなし、健全
- **7.20** PID/1次SMCとの比較で、この極端な持続外乱は**STA固有でなく制御則全般に共通の脆弱性**（PIDが最も早く転倒）と判明。他k1/k2ではz漏れ積分でも転倒を防げず、現行構成のみが頑健
- **7.21** 持続外乱を3アーキタイプの標準検証軸に追加。**副次的発見**: 60秒保持用シナリオのスロットル較正誤差が発覚、較正し直すと§7.18/7.20の「生存」結論の一部が取り下げに（現行構成はそれでも相対的に最頑健）
- **7.22-7.23** 保持時間短縮（60→40秒）、`z_leak_tau`掃引で**1.0s付近に非単調な狭い不安定領域**を発見、**`z_leak_tau=0.5s`（roll/pitch）へ更新**——既存6条件で回帰なし・複数改善
- **7.24** `z_leak_tau=0.5s`実機投入・初飛行、問題なし

### アーク3（§7.25-7.30+続報1-8）`smc_pos_sta`位置制御 再挑戦 → 実機クラッシュ → 持続振動の原因究明

- **7.25-7.26** STA（固定ゲイン）で位置制御を再検討、新規`smc_pos_sta`（レート+速度の汎用`SuperTwisting`構造体）。ラウンド2で速度ループk1/k2を2倍（0.3/0.15→0.6/0.3）に採用
- **7.27** 実機投入・初飛行で「操作性が悪化」——**原因: モード切替（STABILIZE⇔POS_HOLD）時にSTA速度ループがリセットされず**、180秒で35回の意図しない高速切替と重なり切替直後の誤差が10倍以上に。`onModeChange()`にリセット追加で修正
- **7.28**【重要】修正の再飛行で**クラッシュ発生**（バッテリー脱落）。**位置制御SMC/STAの実機投入を中止**——実機の位置制御はPID/`smc_pos`に戻し、`smc_pos_sta`はSILS限定開発へ方針転換
- **7.29** クラッシュ根本原因を特定: 送信機・機体側の2段階デバウンス不足（ボタンチャタリング）。送信機側200ms・機体側3パケット連続一致のデバウンスを追加
- **7.30+続報1-8**【POS_HOLDの持続振動、最大の調査スレッド】現行PIDで約1.73秒周期・3-5cm振幅の非減衰振動を発見。診断計装で発生源を特定:
  - 振動は姿勢/レートループでも位置ループでもなく、**速度ループが姿勢ループの閉ループ動特性を巻き込んで形成する「第3のループ」**の共振傾向（速度ループ単独PM=48.8°、観測周波数3.6rad/sに近い）
  - 続報1-4: モータ遅延精緻化・時間領域4段カスケード再現・SILS自身からのレート同定・推定器遅延（姿勢推定は実測40ms遅延/0.71倍減衰）——いずれも観測周波数を再現できず
  - 続報5: 軸間結合（roll→pitch/yaw）はSILS上完全にゼロと確認、棄却
  - 続報6-7: ESKFのaccel-compα-βトラッカーの加速度状態`a_kin_ned_`が実測で観測周期とほぼ一致（1.66秒 vs 1.73-1.76秒）
  - 続報8【因果関係確定】`eskf.accel_comp.enable=0`で比較検証。**accel-compは振動の原因ではなく抑制側**（無効化で振幅が約4.7倍悪化）、ただし共振周波数には影響する構成要素——**真の根本原因は未特定のまま一時中断**（2026-09-12）。姿勢/レート/位置/速度PIDゲイン自体の変更は本アーク全体を通じて未実施

### アーク4（§7.31-7.49+続報）`smc_rate_asta`適応STA — 大規模チューニングサイクルと安全性の確立

固定ゲインSTA（`smc_rate_sta`）の非単調トレードオフ（§3.7-3.8）に対し、適応スイッチングゲインで解決を試みた長大な反復サイクル。

- **7.31+続報1-4** 不感帯ベース適応則（3ラウンド・110回超のSILS実行）で`torque-authority=0.4`と`noise n1`の同時PASSに**到達できず**。さらに**続報3で過学習を発見**: 2条件だけに最適化した設定が`pos_flight+motor-delay=15ms`で§7.13級の壊滅的破綻を再発——元のシード値（差し戻し後）でも**続報4で同種の破綻**（drift=17.36m, tilt=38.97°）を確認、**適応則自体の構造的欠陥**と判断
- **7.32** 発振検知ゲーティング（ゼロクロス計数）で主破綻は解消したが、**新たな第二の破綻**（飛行末尾でRC入力不変時に2モータが突然duty=0→緊急DISARM）を発見。単純なゼロクロス計数では「発散的振動」と「限界的に安定な小振動」を区別できないと判明
- **7.33**【設計転換】規範モデル（MRAC）方式へ全面転換 — 一次遅れの規範モデルへの追従誤差の速い/遅いEMA比で発散判定。末尾破綻は完全解消。`duty_max=1.0`飽和は**PID/初代SMCにも共通の既知プラント/ミキサー限界**（差動トルク容量不足）と判明、`smc_rate_sta`のみこれを回避
- **7.34** 基本回帰4条件で**過学習の兆候を再発見**（`stab_flight`nominal att_rmse悪化、`pos_flight`nominalでDISARM再発）——1条件への最適化の限界が再度露呈
- **7.35** 発散判定への猶予時間（dwell time）を追加。**実装バグ1件を発見・修正**（`dwell_time=0`境界条件）。しかしトレードオフは解消せず「別の指標へ移動しただけ」と判明
- **7.36**【到達点】振幅比方式から**二重EMAトレンド判定**（MACD型）へ転換。時定数を約1.7倍にスケールし、**基本回帰セット全5シナリオで`smc_rate_sta`とほぼ同等の性能に到達**（`stab_flight`/`stab_combined_aggressive`/`pos_flight+motor-delay=15ms`の3条件PASS、残る2件は`smc_rate_sta`も共有する既知の無関係問題）
- **7.37**【重要・緊急度高】全飛行時間での安全性再検証で**高度ホールドの暴走**を発見（既存チェックは高度を一切見ていなかった）。`smc_rate_sta`（それまで「唯一の実機投入可能な選択肢」）が**11mまで暴走上昇**、`smc_rate_asta`（§7.36版）だけが自己収束——`smc_rate_sta`を無条件に安全とみなす評価を訂正
- **7.38-7.39** duty_max=0.9691改善策を2件試行（k1スルーレート制限／ミキサー側デサチュレーション）——**いずれも効果なし・逆効果**（否定的結果として記録）
- **7.40**【決定】duty_max=0.9691は`pos_flight.scn`が2026-07-26から既知の**プラント/ミキサー容量限界**（差動トルクの構造的不足）と確定。**「現状受容」を正式採用**——PID/初代SMCの完全飽和(1.0)よりは大きく改善済み
- **7.41** 発散ゲートの必要性を検証するアブレーション試験。発散ゲートを無効化するアブレーションで**§7.31/7.32級の破綻が再現**——発散ゲートは安全上必須と再確認。duty_max残差も波形レベルで構造的容量不足と裏付け
- **7.42** 積み残し課題「`noise n2`でPIDだけ離陸未達」を調査。原因は**比例項に測定値フィルタが無いこと**（振動ノイズがそのままトルク指令に乗る）。試作フィルタは離陸未達を解消するが、**無擾乱時のatt_rmseに新規退行**——不採用（既定0=無効のまま記録）
- **7.43-7.49** §7.37の高度暴走の真因を追跡し確定:
  - **7.43** 真因は姿勢制御でなく**高度推定のギャップ**（ToF 4m上限到達＋baro未使用）
  - **7.44-7.45** 実機ホバーログから振動ノイズ`vib_gyro_k`/`vib_accel_k`を再同定（旧値が3-5倍過大と判明）——ただし振動ノイズ自体は§7.42のPID離陸失敗の原因ではなかった
  - **7.46** 真因調査の続報——姿勢傾きは無関係、実duty数%低下という機序仮説を特定するも未証明のまま一旦中断
  - **7.47**【解決・重要】真因確定: **duty飽和の非対称性による平均推力の目減り**——ノイズに依存しない決定論的な再現に成功
  - **7.48-7.49** `smc_rate_asta`・`smc_rate_sta`（固定ゲイン）ともこの非対称duty飽和による離陸失敗をほぼ完全に回避することを確認——STA到達則自体の頑健な性質と確定

### アーク5（§7.50-7.54+続報1-13）`smc_pos_asta`（適応STA・位置まで）— チューニング→実機投入→事故解析→Smith予測器→実機テレメトリ検証

- **7.50** `smc_rate_asta`の適応則を初めて位置/速度ループへ適用（新規`smc_pos_asta`）。初期シードでは固定ゲイン版`smc_pos_sta`に劣る
- **7.51** §7.31-7.36相当のチューニング（`k1_min`0.15→0.3、`mref_trend_floor`0.02→0.3）で**固定ゲイン版と同等以上に到達**（`pos_flight`系drift/att_rmseで軒並み上回る）
- **7.52** 実機投入後のSILS追加検証。新規`modeswitch_rapid.scn`（高速ALT⇔POS連続トグル）作成中に2件の自作ミスを発見・自己修正。修正後は高速トグル・複合外乱いずれも問題なし。実機報告「高出力時にたまに不安定」への対応する再現条件はSILS上で見つからず
- **7.53**【実機・重要】120秒実機飛行中にtilt=156°を検出したが、テレメトリ精査により**制御則の発散ではなくDISARM操作による空中モータカット（自由落下）と確定**——クラッシュ区間を除く残り118秒の飛行データでは一度も真の発散に至らず、制御則自体は健全と確認
- **7.54+続報1-13**【最大の実機起点スレッド】§7.53のログから`sf sysid fit`でレートループを再同定した結果、**`tau_m`が設計時「推定値」の3-6倍**と判明。SILSへ実測遅れを注入して再現実験:
  - 続報1-3【重要・安全性】`pos_flight`は15-30ms間で完全転倒、実測遅れ(~60ms)はその崖の遥か先。既存SILS回帰シナリオ27本を全数マッピングし、崖は`pos_flight`だけでなく`pos_roll/pitch/yaw`系列全体に及ぶと判明
  - 続報4-6 duty容量増強・ゲイン低減はいずれも単純には解決せず。観測器（ESKF）遅れも主要因でないと判断
  - 続報7【重要】記述関数法による遅延マージン理論でレートループ自身を再チューニング——`pos_roll`を完全解決、他も大幅改善
  - 続報9【重要】`pos_flight`の高度暴走の別因を特定・解消（`altitude.vel.kp`自身が同じ「遅延マージン不足」の被害者だった）
  - 続報10【重要】**Smith予測器型のむだ時間補償を実装**（`k1`ゲインは一切変更せず）——`pos_roll`/`pos_pitch`/`stab_combined_aggressive`を完全解決、`pos_flight`もほぼ解決（driftのみゲートに僅かに未達、位置ループ自体の遅延マージンが未着手の最後のピースと特定）
  - 続報11 全8コントローラの横並び比較——Smith予測器付き`smc_pos_asta`だけが健全に飛行、他は全てFAIL（適応STA系は遅延補償なしでむしろ最悪の自励振動を示す）
  - 続報12-13【実機】Smith予測器を実機へ投入、実機テレメトリで**3回・計139秒の健全な飛行**（tilt_max 11-13°）を確認。離着陸時に2回の軽微な接地事故は制御則と無関係と切り分け

### アーク6（§7.55+続報）位置ループ`kp`探索 → **実機危険値と判明し撤回**

現状サマリ#1・#2参照。`kp=1.0`は2026-06-22の実機成長振動→壁激突の値そのものと判明し撤回済み。

### 7.56 「第3のループ」（§7.30）の`smc_pos_asta`向け再検証——Smith予測器の`tau_m`延長では解決せず、否定的結果として記録

以降`smc_pos_asta`のみを対象とする方針（ユーザー確定、2026-09-14）を受け、§7.30が中断したまま残していた「速度ループが姿勢角ループの閉ループ遅れ（PM=48.8°、実効約180ms相当）を無視している」という仮説を`smc_pos_asta`向けに再検証した。§7.30の線形解析はPID速度ループ時代のものであり、STA+Smith予測器に置き換わった現行`smc_pos_asta`にそのまま適用できるかは未検証だった。

**動機となった観測**: §7.55の長時間保持テスト（`pos_gain_deficit_smallnudge_hold40.scn`、kp=0.4基準）で見た緩やかな成長ドリフト（0.047→0.047→0.091→0.105m/40秒）が、§7.30の非減衰振動と質的に似ていた。

**仮説**: 既存のSmith予測器機構（`smc_pos_asta.velx/vely.predictor_tau_m`、現在は実測アクチュエータ遅延60msのみを見込み既定0.06s）に、姿勢角ループ自身の実効遅れ（PM=67.9°@wc=5.53rad/sから逆算≈0.18s）を上乗せすれば、§7.30の症状を同じ仕組みで解決できるのではないか。

**検証**（`--noise off`、実機投入設定=roll/pitch predictor_tau_m 0.062/0.061・leak_tau 1.0、`altitude.vel.kp=0.03`を固定、`velx/vely.predictor_tau_m`を0〜0.42でスイープ）:

| tau_m (velx/vely) | 4分割の推移 | 傾向 |
|---|---|---|
| 0（完全無効） | 0.132→0.181→0.105→0.109m | 現行値より悪化 |
| **0.06（現行実機値）** | 0.079→0.086→0.124→0.089m | 基準 |
| 0.10 | 0.128→0.209→0.160→0.172m | 悪化 |
| 0.15 | 0.074→0.177→0.164→0.123m | 悪化 |
| 0.20 | 0.175→0.206→0.242→0.283m | **明確な成長・最悪** |
| 0.24 | 0.115→0.078→0.131→0.097m | ほぼ同等 |
| 0.30 | 0.090→0.158→0.072→0.125m | ほぼ同等 |
| 0.42（姿勢角遅れ込みの2倍相当） | 0.104→0.220→0.282→0.220m | **明確な成長・悪化** |

`predictor_leak_tau`（velx/vely、tau_m=0.06固定）も0.5/1.0/1.5/2.0/3.0でスイープしたが、同様に非単調・小さな改善と悪化が入り混じり、明確な最適点は見つからなかった。

**結論**: 姿勢角ループの実効遅れを見込んでSmith予測器の`tau_m`を延長するという仮説は**反証された**——延長するとむしろ悪化する条件が複数あり（tau_m=0.20/0.42で明確な成長)、現行値（0.06、実測アクチュエータ遅延のみ）が今回のスイープ内では最良に近い。これは§7.54続報10で確認済みの「Smith予測器はモデルと実際の遅延が一致しない場合に過補正で悪化する」というパターンと整合する——姿勢角ループの遅れは純粋なむだ時間でなく閉ループの緩やかな位相遅れであり、Smith予測器（純遅延モデル）で表現するのが原理的に不適切だった可能性が高い。

**未解決のまま残る点**: 今回の長時間保持テストの緩やかな成長ドリフトが、本当に§7.30の「第3のループ」と同一機序かは確認できていない——`smc_pos_asta`の速度ループは非線形（STA）であり、§7.30の線形Bode解析はそのまま適用できない。真に診断するには§7.30と同様の実測計装（`posdiag`型のログ）を`smc_pos_asta`向けに作り直す必要があるが、本節では未着手。

**変更ファイル**: なし（SILS探索のみ、スクラッチパッド限定）。

**今後の方針**:
- [x] `smc_pos_asta`向けに§7.30型の診断計装（速度ループ入出力・姿勢角追従誤差のログ）を追加し、STAの非線形閉ループでも同種の共振傾向があるか実測で確認する → **§7.56続報で実施**
- [ ] Smith予測器（純遅延モデル）以外の補償形（例: 姿勢角ループの1次遅れ応答を明示的にモデル化した予測器）を検討する余地はあるが、新規設計・SILS検証が要る別テーマ
- [ ] 位置ループ同様、この長時間保持シナリオでの探索もノイズ無効・理想プラントのみで行っている——§7.55の教訓に照らし、本格的な結論を出す前に`--noise`有効化での確認が必要

#### 7.56続報【重要】既存の§7.30診断計装（`pid_controller.cpp`の`posdiag A/B`）が`smc_pos_asta`にもそのまま有効と判明——実測の結果、速度ループ出力に類似の共振傾向を確認

新規計装を書く前に確認したところ、**§7.30で追加した`posdiag A/B`（`pid_controller.cpp`）は削除されずに残っており、`velocity_law_override_`（`smc_pos_asta`のSTAが速度ループを差し替える仕組み）を通しても`ax_ned/ay_ned`・`roll_sp`・`euler.x`（実姿勢）は同じコードパスでログされる**ため、新規計装なしでそのまま使えることが判明した（`pid_controller.cpp:1438-1487`、`:474-488`）。§7.56で使った長時間保持シナリオ（`pos_gain_deficit_smallnudge_hold40.scn`、実機投入設定、tau_m=0.06）をそのまま再実行し、`console.log`の`posdiag A/B`行を後半75%（整定後の保持区間）で解析した。

**解析結果**（ロール右タップのみの単軸擾乱シナリオのため、加振されるのはY軸＝roll系のみ。X軸＝pitch系は終始厳密に0で健全、比較対象外）:

| 信号 | 周期 | 前半rms | 後半rms | rms成長率 | 前半peak | 後半peak | peak成長率 |
|---|---|---|---|---|---|---|---|
| `vy_est`（速度推定、真の物理量に近い） | 1.6s | 0.0546 | 0.0517 | **0.95（減衰）** | 0.138 | 0.132 | 0.96 |
| `ay_ned`（速度ループ＝STAの出力指令） | 1.45s | 0.1349 | 0.2111 | **1.56（成長）** | 0.410 | 0.434 | 1.06 |
| `roll_sp`（同上を傾き指令へ換算） | 1.45s | 0.0138 | 0.0215 | **1.57（成長）** | 0.0418 | 0.0443 | 1.06 |

**発見**: 周期は1.45〜1.6秒——§7.30がPIDで観測した1.73〜1.76秒と完全一致ではないが、**同じ桁・近い帯域**（STA+Smith予測器への置き換えで共振周波数がやや変化したと解釈すれば整合的）。より重要なのは**振幅の非対称な振る舞い**: 制御対象である`vy_est`（速度）自体はむしろ減衰傾向（rms比0.95）で発散していないのに対し、**STAが出している制御指令（`ay_ned`/`roll_sp`）のrmsだけが40秒間で約1.56倍に成長**している——ピーク値はほぼ一定（1.06倍）なので、振幅の絶対的な上限が伸びているのではなく、**振動の「密度」（活性度）が時間とともに増している**、閉ループ不安定というより持続的なチャタリング的活性化に近い波形。

**結論**: §7.30の「第3のループ」と厳密に同一の機序かは断定できないが、**smc_pos_astaにも類似の共振傾向（1.5秒前後の帯域で、制御指令側のエネルギーが持続的に増加する）が実測で存在することが確認できた**——§7.56のパラメータスイープ（tau_m延長）がことごとく効かなかったのは、対象が的外れだったからではなく、**この現象自体がSmith予測器（純遅延の前方予測）で直接手当てできる種類の不安定性ではない**可能性を示唆する。制御対象（速度）自体は発散していないため実害は今のところ限定的（§7.55のPASS判定内に収まる程度）だが、放置すると長時間飛行でノイズ耐性・duty余裕を圧迫するリスクがある。

**変更ファイル**: なし（既存計装の再利用のみ。分析スクリプトはスクラッチパッド限定）。

**今後の方針**:
- [x] この「制御指令側だけが活性化する」パターンの原因を切り分ける——STAの適応則（`k1`の時間変化）自体が主犯か、規範モデルの発散判定（trend_floor等）が持続的に小さくトリガーされ続けているか、実測`k1`の時系列を追加ログして確認する → **§7.56続報2で実施・原因を特定**
- [ ] pitch軸（X軸）も同様に加振するシナリオ（斜め方向のタップ等）で同じ非対称成長が出るか確認し、roll軸固有でないことを検証する
- [ ] ノイズ有効（`--noise n0/n1/n2`）条件でも同じ解析を行い、実機相当のセンサノイズ下でこの活性化傾向がさらに悪化するか確認する

#### 7.56続報2【重要】`k1`の自己活性化を実測で特定——ただし「抑えると悪化」という意外な結果、`leak_ratio=0.4`が唯一の暫定候補

§7.56続報の「今後の方針」を受け、`smc_vel_y_`の`k1`・`e_model_env`・`e_model_env_base`・`z`を新規計装（`posdiag E`、`app_controller.{hpp,cpp}`）で実測した。§7.30の`posdiag A/B`と異なりこちらは新規追加が必要だった（`velocity_law_override_`ラムダ内で`smc_vel_y_.compute()`呼び出し直後）。

**k1の実測挙動**（同一長時間保持シナリオ、既定`dead_band=0.05`・`leak_ratio=0.2`）: 保持開始直後はk1_min付近（0.33〜0.47）で推移するが、**t≈6〜20秒の間に0.41→1.44まで単調に近い形で上昇**し、以降は40秒の終わりまで1.27〜1.46のレンジに張り付いたまま戻らない（`k1_max=1.5`に対し約93〜96%）。1st半分の平均0.83→2nd半分の平均1.36、標準偏差は0.375→0.061へ低下——**振動でなく高いレベルへの定着**であり、これが§7.56続報で見た`ay_ned`/`roll_sp`のrms成長（1.56〜1.57倍）の直接の機序と確認できた。

**メカニズム**（`adaptive_sliding_mode_sta.hpp`のcompute()より）: `k1`は「発散判定中は縮小」「`|s_lpf|`が`dead_band`超過なら`+adapt_rate`で成長」「それ以外は`-adapt_rate*leak_ratio`で漏洩」の3値則。保持中の小さな残留振動が`|s_lpf|`を断続的に`dead_band=0.05`超過させ続け、成長（+adapt_rate）が漏洩（-adapt_rate*leak_ratio=-0.2*adapt_rate、5倍非対称）に負けない頻度で発生し、`k1`がラチェット的に上昇→高止まりする。

**対策の検証（`dead_band`×`leak_ratio`格子、16通り）——意外な結果**: `dead_band`を広げる・`leak_ratio`を上げるといずれも`k1`のプラトーは確かに下がる（例: db=0.15/lr=1.0でk1_plateau=0.312、ほぼk1_min）が、**ほとんどの組み合わせで長時間保持のドリフトはむしろ悪化した**（例: 同じdb=0.15/lr=1.0でquarters=[0.084, 0.249, 0.235, 0.143]、既定[0.064, 0.148, 0.075, 0.076]より明確に悪い）。**高止まりしたk1が、実は小信号の位置保持精度を実際に助けている**——「活性化＝問題」という前提が単純すぎた。

唯一の例外は`dead_band=0.05`（既定のまま）・`leak_ratio=0.4`（既定0.2の2倍）: k1_plateau=1.237（既定1.355よりやや低下）、quarters=[0.101, 0.051, 0.073, 0.048]（既定[0.064, 0.148, 0.075, 0.076]よりQ2以降で明確に改善、final 0.048 vs 0.076）。`pos_flight+motor-delay=60`（既に解決済みのアグレッシブ機動ケース）での回帰確認: drift=2.856m（既定2.82mとほぼ同等）・tilt=19.7°（同一、姿勢角ピークはPOS_HOLD係合前で不変）・duty=0.925（既定0.897よりやや悪化）・att_rmse=2.88°（既定3.05°よりやや改善）——**明確な退行はないが、悪化した指標もある**。

**結論**: `k1`の高止まり自体は「バグ」でなく、STAが小信号レジームで自律的に高ゲインへ移行する適応則の意図した挙動に近い——単純に抑え込むと保持精度が犠牲になる。`leak_ratio=0.4`は唯一プラス方向の候補だが、格子探索のうち1点＋1つの回帰シナリオでしか確認しておらず、§7.31-7.36が繰り返し示した「2条件だけの改善は過学習の危険信号」のパターンにまだ当てはまる可能性がある——**この1点だけでデフォルト変更は提案しない**（CLAUDE.md規定）。

**変更ファイル**:
- `firmware/apps/smc_pos_asta/app_controller.hpp` — `posdiag_counter_`メンバ追加（一時診断、§7.56続報2）
- `firmware/apps/smc_pos_asta/app_controller.cpp` — `<esp_log.h>`インクルード・`TAG`定義・`velocity_law_override_`ラムダ内に`posdiag E`ログ追加（既定動作は無変更、ログ出力のみ）

**今後の方針**:
- [x] `leak_ratio=0.4`（velx/vely）を候補として、`pos_roll`/`pos_pitch`/`pos_yaw`/`modeswitch`/ノイズ有り保持を含む本格的な多シナリオ回帰（§7.31〜7.36相当）で確認する → **§7.56続報3で実施・不採用と判定**
- [ ] `dead_band`/`leak_ratio`以外の軸（`adapt_rate`自体、`mref_trend_floor`）も同じ格子探索に含め、k1プラトーとドリフトの関係をより広く把握する
- [ ] roll軸（pitch側の`smc_vel_x_`）でも同じ`posdiag`計装を追加し、非対称でないか確認する——本節はvely（roll方向の擾乱）のみ計装

#### 7.56続報3【結論】`leak_ratio=0.4`は多シナリオ回帰で不採用——ノイズ有効化で一貫して悪化、§7.55と同型の過学習パターンを再確認

§7.56続報2の暫定候補`leak_ratio=0.4`（velx/vely）を、本計画の標準規律（§7.31-7.36）に沿って`pos_roll`/`pos_pitch`/`pos_yaw`/`modeswitch`＋ノイズ有効化した長時間保持で回帰確認した。

**単軸機動（`--motor-delay 60`、実機投入設定）**:

| シナリオ | 指標 | BASE(leak=0.2) | CAND(leak=0.4) |
|---|---|---|---|
| `pos_roll` | drift/tilt/att_rmse | 1.226m/10.2°/0.81° | 1.222m/10.6°/**1.42°（悪化）** |
| `pos_pitch` | drift/tilt/att_rmse | 1.343m/**24.6°**/3.15° | 1.344m/**20.3°（改善）**/2.46°（改善） |
| `pos_yaw`（既知xfail、両方drift/tiltゲート大幅未達） | drift/tilt/att_rmse | 12.069m/21.4°/5.19° | 13.738m/25.2°/6.80°（**全指標悪化**） |

**`modeswitch`（motor-delay無し）**: drift 0.758→0.809m、att_rmse 0.64→0.83°——軽微だが一貫して悪化（どちらもゲート内）。

**長時間保持＋ノイズ有効**（§7.55の教訓を踏まえ`--noise n0/n1/n2`×2シード＝6条件、4分割ドリフトの最終値で比較）:

| noise | seed | BASE最終値 | CAND最終値 |
|---|---|---|---|
| n0 | 12345 | 0.078m | 0.101m（悪化） |
| n0 | 777 | 0.050m | **0.130m（2.6倍悪化）** |
| n1 | 12345 | 0.345m | 0.360m（悪化） |
| n1 | 777 | 0.554m | 0.563m（悪化） |
| n2 | 12345 | 0.252m | 0.289m（悪化） |
| n2 | 777 | 0.243m | 0.290m（悪化） |

**結論**: `pos_pitch`では改善したが`pos_roll`/`pos_yaw`/`modeswitch`では悪化、**ノイズを有効にした6条件全てでBASEを上回れなかった**（1条件は2.6倍の明確な悪化）。§7.56続報2で見えた「長時間保持1シナリオ・ノイズ無効」での改善は、ノイズ有効・複数シナリオという条件を広げた途端に再現しなかった——これは§7.55（位置ループkp探索）・§3.1（yaw.phi）・§7.31続報2-4と全く同型の過学習パターンである。**`leak_ratio=0.4`は不採用**とし、`params.cpp`のデフォルト（0.2）は変更しない。

**総括（§7.56系列）**: 「第3のループ」の機序（k1の自己ラチェット）は実測で特定できたが、それを安全に緩和するパラメータは今回の探索範囲では見つからなかった——現行デフォルトが、試した中では最も広い条件でバランスが取れている。

**変更ファイル**: なし（探索のみ、コードは§7.56続報2の診断計装から不変）。

**今後の方針**:
- [x] `dead_band`/`leak_ratio`以外の軸（`adapt_rate`、`mref_trend_floor`）での格子探索——ただし今回同様、必ずノイズ有効＋多シナリオでの検証を条件に含めること（単一シナリオ・ノイズ無効での「改善」は判断材料にしない） → **§7.56続報4で実施、どちらも候補なしと判明**
- [ ] `pos_yaw`の悪化（12.1→13.7m）は既知xfail（yawトルク権限飽和）の範囲内での変化か、`leak_ratio`固有の新規影響かを切り分ける余地がある——優先度は低い
- [ ] 診断計装（`posdiag E`）はコードに残置——将来の同種調査で再利用可能

#### 7.56続報4【§7.56系列の区切り】`adapt_rate`・`mref_trend_floor`探索——`mref_trend_floor`は本症状に無関係と判明、`adapt_rate`は現行値が既に最良、パラメータ探索の道を尽くす

§7.56続報3の教訓（ノイズ無効・単一シナリオの「改善」は当てにならない）を最初から適用し、今回は**ノイズ有効(`n1`)と無効(`off`)を両方同時に見ながら**`adapt_rate`・`mref_trend_floor`（velx/vely）をスクリーニングした。

**`mref_trend_floor`スイープ（0.15〜0.6、既定0.3）**: **5点全てでoff/n1とも完全に同一の数値**（小数点4桁まで一致）。この長時間保持シナリオでは「発散判定」（`e_model_env - e_model_env_base > mref_trend_floor`）が一度も真にならず、`k1`の成長は専らもう一方の分岐（`|s_lpf| > dead_band`）だけで起きていることが確定した——**`mref_trend_floor`は本症状（k1ラチェット）と無関係**、これ以上このパラメータを触っても無意味。

**`adapt_rate`スイープ（0.1〜0.4、既定0.2）**:

| adapt_rate | off最終値 | n1最終値 |
|---|---|---|
| 0.1 | 0.170（悪化） | 0.381（悪化） |
| 0.15 | 0.207（悪化） | 0.395（悪化） |
| **0.2（既定）** | **0.076（最良）** | **0.345（同率最良）** |
| 0.3 | 0.083（ほぼ同等） | 0.377（悪化） |
| 0.4 | 0.114（悪化） | 0.345（同率最良） |

既定値0.2が、off/n1どちらでも最良または同率最良——**現行デフォルトが既にこの探索範囲内で最適**。これは構造的にも整合する: `adapt_rate`は成長則（`+adapt_rate`）・漏洩則（`-adapt_rate*leak_ratio`）の**両方に共通の係数**として掛かっているため、`adapt_rate`単独のスケーリングは成長/漏洩の**比率**を変えず（比率を変えるのは`leak_ratio`）、到達するk1プラトーの高さでなく時定数（速さ）だけを変える——§7.56続報2-3で`leak_ratio`（比率そのもの）を動かしても改善しなかったことと合わせ、この症状に対して`adapt_rate`が原理的に効きにくいことの傍証となる。

**結論（§7.56系列の総括）**: `dead_band`・`leak_ratio`・`adapt_rate`・`mref_trend_floor`の4パラメータ全てを（後半2つはノイズ有効を含めて）探索したが、**現行デフォルトを安全に上回る設定は一つも見つからなかった**。「第3のループ」に類する共振傾向（k1の自己ラチェット）の機序は実測で特定済みだが、既存の適応則パラメータの再チューニングでは解決しない——**これ以上の同型パラメータ探索は収穫逓減と判断し、§7.56系列のパラメータ探索はここで打ち切る**。

**変更ファイル**: なし。

**今後の方針**:
- [ ] パラメータ再チューニングでなく、適応則の**構造自体**を見直す方向（例: 成長/漏洩を非対称な定数比でなく`|s_lpf|`の大きさに比例させる、発散判定を振幅比でなくエネルギー積分で見る等）は未着手——ただし新規設計＋SILS検証が要る別テーマであり、§7.31-7.36相当の投資に見合うかはユーザー判断
- [x] 現状の`k1`高止まりは実害（duty飽和・ノイズ耐性の圧迫）に至っていないため、実機投入判断には影響しない——ただし長時間飛行での監視項目として記録しておく価値がある → **§7.56続報5で「実害なし」の前提そのものが誤りと判明、撤回**

#### 7.56続報5【重要・§7.56続報2の前提を撤回】`k1`診断そのものがノイズ無効のみで行われていた——ノイズ有効化で`k1`はほぼ常時上限張り付きと判明、severityを大幅に過小評価していた

§7.55の教訓（ノイズ無効・理想プラントのみでの「改善」判定は実機で反証されうる）に照らし、本格的な結論を出す前に`--noise`有効化での確認が必要という観点からの再検証。§7.56続報3/4の**パラメータ比較**はノイズ有効・無効を両方見ていたが、**§7.56続報2の`k1`挙動そのものの診断**（「0.47→1.44まで20秒かけて緩やかに上昇し高止まり」）は**ノイズ無効のみ**で行っており、この一次診断自体が§7.55と同型の落とし穴に入っていた。

**再検証**（既定設定`dead_band=0.05`/`leak_ratio=0.2`固定、`--noise off/n0/n1/n2`×2シード、`posdiag E`の`k1`を再解析）:

| noise | k1（1st半分平均） | k1（2nd半分平均） | k1_max実測 | k1が上限近傍(>1.45)の時間割合 |
|---|---|---|---|---|
| off（§7.56続報2で使用） | 0.827 | 1.364 | 1.484 | 5% |
| **n0**（最も軽いノイズ） | **1.249〜1.255** | **1.492** | **1.500（上限到達）** | **65〜72%** |
| **n1** | **1.489〜1.491** | **1.494** | **1.500** | **98〜99%** |
| **n2** | **1.454〜1.483** | **1.484〜1.495** | **1.500** | **85〜98%** |

**発見**: ノイズ無効では「20秒かけて緩やかに上昇し1.36〜1.48で高止まり」という**穏やかな成長**だったのに対し、**ノイズを有効にした瞬間（最も軽いn0でさえ）、`k1`は保持区間の前半からすでに1.25前後、後半には上限1.5にほぼ完全に張り付く**——「緩やかなラチェット」ではなく「常時ほぼ上限で運用されている」という、質的に異なる・はるかに深刻な状態だった。§7.56続報2の一次診断は severity を大幅に過小評価していたことになる。

**`mref_trend_floor`の「無関係」判定はノイズ下でも維持**: `n1`条件で`e_model_env - e_model_env_base`（発散判定に使うトレンド量）を直接確認したところ、704サンプル中、既定`mref_trend_floor=0.3`を超えたのは**0件**（観測最大0.0647）——§7.56続報4の「発散判定は一度も発火せず`mref_trend_floor`は無関係」という結論は、ノイズ有効下でも変わらず成立する。

**§7.56続報3/4の結論への影響**: `leak_ratio`/`adapt_rate`のパラメータ比較実験自体はノイズ有効条件を含んでいたため、「どの候補も現行デフォルトを上回らない」という**結論は変わらない**——今回分かったのは「現行デフォルトを含め、ノイズ下では`k1`が常時上限近くで運用されている」という**severityの評価**であり、既存の比較結果を覆すものではない。ただし「実害に至っていないため優先度は低い」（続報4の今後の方針）という**判断は撤回**する——`k1_max`の実測余白がノイズ下でほぼゼロ（既に上限で運用中）ということは、実機で真に大きな外乱が来た際に**適応則がそれ以上ゲインを上げる余地がない**ことを意味し、これは監視項目でなく検討すべき論点に格上げすべき。

**未検証のまま残る新たな問い**: `k1_max`自体（現行1.5）を上げると、単純にさらに高い値へ張り付くだけなのか、それともプラトーが別の（より穏当な）水準に落ち着くのかは今回未確認——次に検証する価値がある具体的な問い。

**変更ファイル**: なし（既存計装での再解析のみ）。

**今後の方針**:
- [x] `k1_max`（velx/vely、現行1.5）を上げた場合の挙動をノイズ有効下で確認する——張り付き先が単純に上限に追従するだけか、別の力学があるかを切り分ける → **§7.56続報6で実施**
- [x] duty_max・モータ飽和への実際の影響を、ノイズ有効条件下の`pos_flight`等でも定量確認する → **§7.56続報6で実施**
- [x] 本節の教訓を一般化: **診断計装による一次特性把握そのものも、パラメータ比較実験と同じくノイズ有効を含めて行うことを今後の標準手順とする** → 以降の全節（続報6含む）で適用済み

#### 7.56続報6【§7.56系列 最終】`k1_max`引き上げ——`k1`は無制限に成長せず自然な均衡点(~3.0-3.5)に落ち着くと判明（安心材料）が、改善はノイズ条件依存で汎化せず。パラメータ探索を完全に打ち切る

§7.56続報5の問い（`k1_max`を上げるとどうなるか）を検証した。`k1_max`（velx/vely、既定1.5）を1.5/2.0/3.0/5.0/10.0でスイープし、`--noise off`と`n1`（2シード）を最初から併用した。

**`k1`の均衡点についての安心材料**: `k1_max=1.5/2.0/3.0`では`k1`がほぼ毎回その上限に張り付く（例: k1_max=3.0でプラトー2.79〜2.95）が、**`k1_max=5.0`にすると新たな均衡点（~3.0〜3.5）で頭打ちになり、`k1_max=10.0`にしても全く同じ結果**（プラトー・quarters・duty全て`k1_max=5.0`と完全一致）——`k1`は上限を上げれば上げるほど無制限に成長するわけではなく、成長則・漏洩則自身の力学から決まる**自然な均衡点（このノイズ条件では~3.0-3.5）が存在する**。`k1_max`を野放図に上げても暴走しないことが確認でき、これは安全上の安心材料。

**`k1_max=3.0`候補の多条件検証（ノイズ全4段階＋`pos_flight+motor-delay=60`）**:

| 条件 | BASE(k1_max=1.5)最終値 | CAND(k1_max=3.0)最終値 | 判定 |
|---|---|---|---|
| noise=n1, seed=12345 | 0.345 | 0.320 | 改善 |
| noise=n1, seed=777 | 0.554 | 0.357〜0.393 | **明確に改善** |
| noise=n0, seed=12345 | 0.078 | 0.124 | **悪化** |
| noise=n0, seed=777 | 0.050 | 0.058 | 悪化 |
| noise=n2, seed=12345 | 0.252 | 0.260 | ほぼ同等 |
| noise=n2, seed=777 | 0.243 | 0.159 | 改善 |
| `pos_flight+motor-delay=60` | drift 2.821m/duty 0.897/att_rmse 3.05° | drift 2.821m/duty **0.914**/att_rmse 2.76° | ほぼ同等（duty微増・att_rmse微減） |

**結論**: `k1_max=3.0`は`n1`では明確に改善するが、**`n0`では一貫して悪化し、`n2`では条件依存**——4段階のノイズレベル全てで一貫して改善する設定ではなかった。これは§7.56系列を通じて繰り返し見てきたパターン（特定条件だけで効く・広げると崩れる）そのもの。`pos_flight`側は明確な退行はない。

**§7.56系列の最終結論**: `dead_band`・`leak_ratio`・`adapt_rate`・`mref_trend_floor`・`k1_max`——STA適応則の主要パラメータをひとしきり探索したが、**全ノイズ条件で一貫して現行デフォルトを上回る設定は一つも見つからなかった**。唯一の実質的な収穫は「第3のループ」に類する共振傾向の機序（`k1`の自己ラチェット、ノイズ下ではほぼ常時上限運用）の特定と、「`k1_max`を上げても暴走しない」という安全上の確認。**これ以上の同型パラメータ探索は行わない**——次に意味があるとすれば、適応則の構造自体の再設計（§7.56続報4で既述）のみ。

**変更ファイル**: なし。

**今後の方針**:
- [x] 適応則の構造再設計（成長/漏洩を`|s_lpf|`の大きさに比例させる等）は新規テーマとして独立に検討する → **§7.57で実施**
- [ ] ノイズレベル依存で結果が反転する現象自体（n1で改善・n0で悪化）の理由を理解する価値はあるかもしれないが、優先度は低い

### 7.57 適応則の構造再設計（比例成長/漏洩・エネルギー積分発散判定）——両方試したが改善せず、否定的結果として記録

§7.56系列で特定した機序（`k1`が`dead_band`超過のたびbang-bang的に固定速度で成長し、ノイズ下ではほぼ常時上限に張り付く）への構造的対処を2つ実装・検証した。

**実装**（`adaptive_sliding_mode_sta.hpp`、`smc_pos_asta.{velx,vely}`のみ、両方とも既定0=無効で既存の厳密なbang-bang/振幅エンベロープ判定にフォールバック——バイト同一を確認済み）:

1. **比例成長/漏洩**（新規`prop_adapt_gain`、既定0）: `k1_dot = prop_adapt_gain * (|s_lpf| - dead_band)`という単一の連続則で、固定`+adapt_rate`/`-adapt_rate*leak_ratio`の組を置き換え——`dead_band`超過分の大きさに比例して成長・不足分の大きさに比例して漏洩する。ノイズ級の僅かな超過は小さな押し上げに、真の外乱は速い成長になるはずという狙い
2. **エネルギー積分発散判定**（新規`energy_trend_floor`、既定0）: 既存の`e_model_env - e_model_env_base`（`|e_model|`のエンベロープ差）による発散判定を、`e_model^2`（エネルギー/パワー）のエンベロープ差に置き換え可能にした——同じ2つの時定数（`mref_env_tau`/`mref_env_base_tau`）を流用し、`e_energy_fast`/`e_energy_slow`という新規状態で並行計算（既定判定と同時に常時更新、切替に状態リセット不要）

**検証1（`prop_adapt_gain`スイープ、1〜16、`--noise off`と`n1`を最初から併用）**: **全ての値・全ての条件でbang-bang版の既定（`prop_adapt_gain=0`）を下回る結果はなく、むしろ大半が悪化した**（例: noise=off・既定final=0.076に対し、`prop_adapt_gain`=1〜16はいずれも0.10〜0.30台まで悪化。n1でも改善は散発的で一貫しない）。

**検証2（`energy_trend_floor`スイープ、0.001〜0.1、同上）**: 低い値（0.001, 0.003）では発散判定が過敏になりすぎ、n1条件で**quarters最大3.05mという壊滅的な悪化**（頻繁な誤発火によるk1の不必要な縮小と推測）。値を上げていくと徐々に既定挙動（0.1で判定が一度も発火せず既存bang-bang版と完全一致）に収束するのみで、**改善する中間値は一つも見つからなかった**。

**結論**: 提案された2つの構造再設計（比例則・エネルギー積分発散判定）はいずれも、探索した範囲では既定のbang-bang/振幅エンベロープ版を上回らなかった——比例則はほぼ全条件で悪化、エネルギー積分判定は「過敏で壊滅的」か「既定と同じ」の二択で中間の改善域が見つからない。§7.56系列のパラメータ探索と合わせ、**この適応則ファミリー（bang-bang成長/漏洩＋トレンド判定という設計パターン自体）の範囲内では、ノイズ下でのk1高止まりを安全に緩和する道が見つからない**という、より強い結論に達した。

**変更ファイル**:
- `firmware/apps/smc_pos_asta/adaptive_sliding_mode_sta.hpp` — `prop_adapt_gain`/`energy_trend_floor`パラメータ、`e_energy_fast`/`e_energy_slow`状態を追加（既定0=無効、`reset()`にも追加）
- `firmware/apps/smc_pos_asta/app_controller.cpp` — `velx`/`vely`のparam読み込みに追加
- `firmware/vehicle/components/sf_core/params.cpp` — `smc_pos_asta.{velx,vely}.{prop_adapt_gain,energy_trend_floor}`を追加（宣言・テーブル両方、既定0.0f）

**今後の方針**:
- [x] 新規コード（`prop_adapt_gain`/`energy_trend_floor`）は既定無効のまま残置——`k1_slew_max`（§7.38）と同じ「否定的結果の記録」慣行。削除せず将来の再検証の出発点とする
- [x] このファミリー内での探索は事実上尽きた——次に意味があるとすれば、適応則そのものではなく速度ループの外側（位置ループ）や、まったく異なる到達則設計（例: バリア関数ベースの適応、固定時間収束SMC等）だが、いずれも本計画のスコープを超える新規研究テーマ → **`filter_tau`（ノイズをdead_band判定に入る前に削る方向）を§7.57続報で追加検証**
- [ ] 実機ビルド（ESP32-S3向け）は本節も含め未確認——このセッションはSILS（MinGWホストビルド）のみで検証しており、次回セッションで確認が必要

#### 7.57続報 ノイズ低減アプローチの検証——`filter_tau`（判定前のLPF強化）を試すも、総当たりで既定を上回らず

適応則への反応の仕方（dead_band/leak_ratio/adapt_rate等）でなく、**判定に使う`s_lpf`自体のフィルタ強度**（`filter_tau`、既定0.05s、§7.31で「`|s|`でなく`s`を先にフィルタすべき」と確定した既存機構）という、本系列で唯一まだ振っていなかったレバーを検証した。

**粗いスクリーニング**（`filter_tau`=0.05〜0.5、`--noise off`/`n1`併用、長時間保持）: `filter_tau`を上げるとn1条件での`k1`の上限近傍滞在率が明確に単調減少した（0.05: 98-99%→0.5: 63-72%）——ノイズをより強く削れば判定の誤発火が減るという狙い通りの効果を確認。ドリフトもn1の一部条件（seed=777、`filter_tau=0.2`で0.554→0.474）で改善が見られ、`pos_flight+motor-delay=60`回帰も`filter_tau=0.1`まではほぼ無傷（drift 2.821m同一、att_rmse 3.05°→2.95°）——**`filter_tau=0.1`が唯一、粗いスクリーニングで明確な退行を示さなかった候補**だったため、本格回帰へ進んだ。

**総当たり結果**（`pos_roll`/`pos_pitch`/`pos_yaw`/`modeswitch`＋長時間保持×ノイズ全4段階×2シード）:

| シナリオ | 結果 |
|---|---|
| `pos_roll` | drift/att_rmse改善、tilt悪化（10.2°→12.6°、ゲート18°内） |
| `pos_pitch` | drift/tilt/att_rmse**全て改善**（tilt 24.6°→22.9°、既知FAILの範囲内での改善） |
| `pos_yaw`（既知xfail） | drift 12.1→10.1m・att_rmse 5.19→4.99°、いずれも改善 |
| `modeswitch` | drift悪化（0.758→0.879m、ゲート2.0m内）、att_rmse改善 |
| 長時間保持 noise=**n0**（2シード） | **明確に悪化**（0.078→0.168、0.050→0.082、いずれも倍以上） |
| 長時間保持 noise=off/n1/n2（6条件） | ほぼ同等（プラスマイナス数%の範囲、明確な傾向なし） |

**結論**: 単軸機動（`pos_roll`/`pos_pitch`/`pos_yaw`）では一貫して改善したが、**本来の標的である長時間保持シナリオでは`noise=n0`（最も軽いノイズ）で明確に悪化し、他のノイズ条件も改善と呼べる水準ではなかった**——粗いスクリーニングで「唯一退行なし」に見えた`filter_tau=0.1`も、条件を総当たりで広げると退行が見つかった。これで`dead_band`・`leak_ratio`・`adapt_rate`・`mref_trend_floor`・`k1_max`・`prop_adapt_gain`・`energy_trend_floor`・`filter_tau`——**この適応則が持つほぼ全てのチューニング可能なレバーを試し尽くした**ことになる。「ノイズを判定前に削る」というアプローチ自体は理にかなっており（`k1`の上限滞在率は確かに下がった）、単軸機動には副次的な恩恵もあったが、**長時間保持という本来の標的に対しては解決策にならなかった**。

**変更ファイル**: なし（既存`filter_tau`パラメータでの探索のみ、コード変更なし）。

**今後の方針**:
- [ ] `filter_tau=0.1`は単軸機動（pos_roll/pitch/yaw）でのみ副次的な改善が見られたため、長時間保持と無関係な文脈（例: motor-delay注入時のみ）で単独採用する余地はゼロではないが、優先度は低い
- [ ] 本系列（§7.30・§7.56・§7.57）のパラメータ・構造探索は実質的に完了——これ以上は新規の到達則設計（本計画のスコープ外）でない限り収穫は見込めない

## 4. 実機投入ゲート

上記SILS検証手順が全てクリアし、かつ**ユーザーの明示的な判断**を得てから初めて
`sf app build smc_rate` / `sf app flash smc_rate -m`に進む。development_roadmap.mdの層別検証
（ACRO→STABILIZE→…）の思想を踏襲し、まずACROのみ・ベンチ/テザー拘束下で確認する。
SMCは既存の線形ETFEモデル（`rate_sysid.py`の`G(s)=b·e^{-Ls}/(s(Ts+1))`）による同定を前提とした
±20%/±50%ゲート（development_roadmap.md Phase 3/4）がそのまま適用できるとは限らない点に留意する
（非線形制御則のため）— 実機投入判断はSILSの数値結果を提示した上でユーザーと個別に協議する。

## 5. 進捗管理（やったこと／やっていないこと）

> 本節は各節の末尾に散在する「今後の方針」チェックリスト（§3.x/§7.x、計20件超）を集約した
> 単一の状態表。**個別のチェックボックスと矛盾する場合は本節を正とする**
> （例: 旧§7.56続報4の項目はテキストが「未着手」なのにチェック済みになっていた不整合を
> 2026-09-15に本節で訂正）。スコープは2026-09-14より`smc_pos_asta`に限定（現状サマリ#5）——
> 他コントローラの項目は「スコープ外・参考記録」表に分離した。

### やったこと（主要マイルストーン）

| # | 内容 | 結果 | 節 |
|---|---|---|---|
| 1 | `smc_rate`（1次SMC・レートのみ）実装・チューニング・PID A/B比較・ACRO実機投入 | 完了 | §3.1-3.9 |
| 2 | `smc_pos`（1次SMC・位置まで）検証 | 完了 → PIDを明確に上回れず見送り | アーク1 §7.1-7.9 |
| 3 | `smc_rate_sta`（固定STA・レートのみ）実装・z漏れ積分安全機構・実機投入 | 完了・実機投入済み | アーク2 §7.10-7.24 |
| 4 | `smc_pos_sta`（固定STA・位置まで）検証 | 完了 → 実機クラッシュ後SILS限定に方針転換 | アーク3 §7.25-7.28 |
| 5 | 「第3のループ」（速度×姿勢角ループ共振、PID時代）原因調査 | 中断 → 真因未特定のまま§7.56へ引き継ぎ | §7.30+続報1-8 |
| 6 | `smc_rate_asta`（適応STA・レートのみ）チューニング・安全機構確立・duty限界受容 | 完了 | アーク4 §7.31-7.49 |
| 7 | `smc_pos_asta`チューニング・実機投入・事故解析（DISARM誤検知と確定） | 完了 | アーク5 §7.50-7.53 |
| 8 | レートループ実遅延の実機同定（`tau_m`が設計値の3-6倍と判明） | 完了 | §7.54 |
| 9 | Smith予測器型むだ時間補償の設計・実装・SILS検証 | 完了 | §7.54続報7/10 |
| 10 | Smith予測器の実機投入・実機テレメトリ検証（3回・計139秒、健全） | 完了 | §7.54続報12-13 |
| 11 | 位置ループ`kp`探索 | 完了 → 実機危険値（2026-06-22事故値）と判明し撤回 | アーク6 §7.55 |
| 12 | 「第3のループ」の`smc_pos_asta`向け再検証（Smith予測器`tau_m`延長） | 完了 → 反証（否定的結果として記録） | §7.56 |
| 13 | 診断計装（`posdiag A/B`流用）による共振傾向の実測確認 | 完了 → 制御指令側のrms成長を確認 | §7.56続報 |
| 14 | `k1`自己ラチェットの機序特定（新規`posdiag E`計装） | 完了 | §7.56続報2 |
| 15 | `leak_ratio=0.4`の多シナリオ・ノイズ有効回帰確認 | 完了 → 不採用 | §7.56続報3 |
| 16 | `adapt_rate`/`mref_trend_floor`の格子探索（ノイズ有効込み） | 完了 → 候補なし、パラメータ探索は打ち切り | §7.56続報4 |
| 17 | §7.56続報2の`k1`診断そのものをノイズ有効下で再検証 | 完了 → **severityを大幅に過小評価していたと判明**（ノイズ下ではほぼ常時上限張り付き） | §7.56続報5 |
| 18 | `k1_max`（1.5〜10.0）引き上げの挙動・多条件検証をノイズ有効下で実施 | 完了 → **`k1`は無制限に成長せず自然な均衡点(~3.0-3.5)に落ち着くと判明（安全上の安心材料）**。ただし改善はノイズ条件依存（n1で改善/n0で悪化）で汎化せず、**現行デフォルト（k1_max=1.5）を安全に上回る設定は見つからず**——§7.56系列のパラメータ探索を完全に打ち切り | §7.56続報6 |
| 19 | README.mdに参考文献（R1-R11+3件）・制御構造ブロック線図（Mermaid）を追加 | 完了 | README.md |

**§7.56系列の総括**（続報6で確定）: `dead_band`・`leak_ratio`・`adapt_rate`・`mref_trend_floor`・`k1_max`という
STA適応則の主要パラメータを、後半はノイズ有効を標準として一通り探索したが、**全ノイズ条件で一貫して
現行デフォルトを上回る設定は一つも見つからなかった**。実質的な収穫は「第3のループ」の機序
（`k1`の自己ラチェット、ノイズ下でほぼ常時上限運用、ただし上限自体は無制限暴走しない）の特定のみ——
**同型のパラメータ探索はここで完全に終了**。次に意味があるとすれば下表#1の構造再設計のみ。

### やっていないこと（`smc_pos_asta`スコープ内・未着手）

| # | 内容 | 優先度 | 節 |
|---|---|---|---|
| 1 | 適応則の**構造自体**の見直し（成長/漏洩を`|s_lpf|`の大きさに比例させる、発散判定をエネルギー積分にする等） | 中（新規設計＋SILS検証が要る別テーマ、投資判断はユーザー——§7.56系列が完全終了した今、唯一残る発展方向） | §7.56続報4/6 |
| 2 | ノイズ有効条件下の`pos_flight`等で、`k1`常時上限張り付き（k1_max=1.5のまま）がduty_max/モータ飽和に与える実際の影響を定量確認 | 中（§7.56続報6のpos_flight単発チェックでは明確な退行なしだが、ノイズ込みでの網羅確認はまだ） | §7.56続報5/6 |
| 3 | ノイズレベル依存で結果が反転する現象自体（`k1_max=3.0`はn1で改善・n0で悪化）の理由を理解する | 低 | §7.56続報6 |
| 4 | pitch軸（`smc_vel_x_`）でも`posdiag`計装で同じ非対称成長を確認——roll軸固有でないか検証 | 低（§7.56系列終了により優先度低下） | §7.56続報 |
| 5 | `pos_yaw`悪化（`leak_ratio=0.4`時、12.1→13.7m）が既知xfail範囲内か新規影響かの切り分け | 低（`leak_ratio=0.4`自体が不採用のため実害は限定的） | §7.56続報3 |
| 6 | `poshold_journey.md`§8の残存2件（1Hzフラフラ＝フロー速度ノイズのvel.kp増幅／高度長周期上下動＝モータ応答遅れ110ms） | 中（ハード限界内で現実的な次候補、§7.56系列と独立） | poshold_journey.md§8 |
| 7 | Smith予測器（純遅延モデル）以外の補償形（姿勢角ループの1次遅れを明示モデル化した予測器）の検討 | 低（新規設計テーマ） | §7.56 |
| 8 | 診断・比較実験の標準手順を「ノイズ有効込みを必須」に一般化（一次診断もノイズ無効だけでは不十分になりうるという教訓の運用への反映） | 中（プロセス改善、§7.56続報4/6は既にこれに沿って実施済み） | §7.56続報5 |

### スコープ外・参考記録のみ（他コントローラ／PIDベースライン）

| # | 内容 | 節 |
|---|---|---|
| 1 | `noise n2`でPIDだけ離陸未達の原因未解明——`smc_pos_asta`とは無関係の別スレッド | §3.2 |
| 2 | `smc_rate_asta`の実機投入可否——`duty_max`受容判断を踏まえた別途のユーザー承認が未実施（スコープが`smc_pos_asta`へ移ったため現状は優先度低下） | §7.40/7.41 |

実機投入ゲート運用は§4参照。

## 6. 参考文献

本計画の制御則設計（§2.2到達則、§2.2超平面根拠、§3.4提案）が依拠する文献。Web検索で
書誌情報を確認済み（2026-09-11）。

| ID | 文献 | 本計画での使用箇所 |
|----|------|-------------------|
| R1 | W. Gao and J. C. Hung, "Variable structure control of nonlinear systems: a new approach," *IEEE Transactions on Industrial Electronics*, vol. 40, no. 1, pp. 45–55, 1993. | §2.2の到達則`τ = I·(k·sat(s/φ)+η·s)`（"constant-plus-proportional rate reaching law" / exponential reaching law）の根拠 |
| R2 | J.-J. E. Slotine and W. Li, *Applied Nonlinear Control*, Prentice Hall, 1991. | 境界層（`sign()`→`sat()`置換）によるチャタリング抑制の標準的手法。§2.2の`φ`（境界層半幅）と、本文冒頭で説明した相対次数と超平面設計（`s=ė+λe`型）の一般論の教科書的典拠 |
| R3 | V. Utkin and J. Shi, "Integral sliding mode in systems operating under uncertainty conditions," in *Proc. 35th IEEE Conference on Decision and Control (CDC)*, Kobe, Japan, 1996, pp. 4591–4596. | §3.4で提案した積分項付加の着想元（Integral Sliding Mode, ISM）。**用語の精度に関する注記**: Utkin & Shiの原論文のISMは、システムの次数を保存し初期時刻からスライディングモードを成立させる（到達フェーズそのものを除去する）より精緻な構成であり、§3.4で提案した単純な加算型`s = e + λ_i∫e dt`（実務上「PI型スライディング面」と呼ばれる簡略版）とは厳密には異なる。本計画で実装を検討する場合はPI型スライディング面（簡略版）であり、それを指してISMと呼ぶのは不正確——§3.4の文中表現もこの区別に従う |

補足: §3.4で言及した「PI型スライディング面」は無人機（クアッドロータ）の姿勢制御分野でも
広く実用されている（例: 風外乱下のクアッドロータ姿勢制御にADRC+ISMCを組み合わせた研究、
*Mechanical Systems and Signal Processing*, vol. 129, 2019, 著者名の完全なリストは未確認
のため書誌情報として確定はしていない — 個別の設計判断はR1〜R3の確定済み3件に基づく）。
