# 講習会でのペアリング取り違え対策（方式の比較と提案）

作成: 2026-09-11。**状態: 決定（2026-09-11 ユーザー確認）。実装中。**

**決定（2026-09-11）:** §3.1 の **W3（コントローラが受信した機体の MAC 下 4 桁を画面に一覧表示し、
利用者が複数候補から選んで確定する方式）のみを採用**する。ペアコード（W2）、近接自動選択（W1）、
Grove ケーブル（C1）、コントローラ CLI + `sf pair`（P1）は今回は見送る。W3 だけで講習会の運用は
実用上ほぼ問題なく成立する、との判断。

**電文は変更しない:** 現行のペアリング電文は機体の完全な MAC を含み、操縦電文は宛先機体の MAC
下 3 バイト（`drone_mac`）を含む。W3 に必要なのは (1) コントローラが「最初の 1 通」ではなく候補を
集めて画面で選ばせること、(2) 機体がペアリング中も「自分宛（`drone_mac` 一致）の操縦電文」だけを
相手候補にすること、の 2 点であり、§4.1 の電文 v2 は不要になった（§4 の実装設計を参照）。

発端: 講習会のように多人数が同じ部屋で同時にペアリングすると、隣のセットと取り違えて
意図しない組（クロスペアリング）になる。現行の唯一の対策は「1 組ずつ順番に、他は電源
OFF」という運用ルールで、参加者が同時に操作する講習では守りきれない。

方針の前提（ユーザー指示 2026-09-11）: 無線だけで解決する方式、有線で両機をつなぐ方式、
コントローラにも CLI を付けて双方 CLI でペアリングする方式、複数の方式を併存させる方式を
それぞれ検討する。

## 0. 要旨

| 観点 | 内容 |
|------|------|
| 取り違えの原因 | 機体は「ペアリング中に最初に届いた操縦電文の送信元」を、コントローラは「最初に受信した署名付きペアリング電文の送信元」を、それぞれ無条件に相手と確定する。電文には「StampFly である」ことを示す固定署名しか無く、「どの機体・どのコントローラか」を表す情報が無い。設計文書 `firmware/vehicle/docs/pairing_plan.md` の P4（未着手）に既知の弱点として明記済み |
| 根本対策の共通基盤 | どの方式でも最後は「コントローラが相手機体の MAC を知り、機体は自分宛（`drone_mac` 一致）の電文だけを受理する」形にする（**宛先指定ペアリング**）。ペアリング電文を v2 に拡張し、識別子（ペアコードまたは宛先 MAC）を載せる |
| 提案する組合せ | 層 1（既定・追加ハード不要）: コントローラの画面に受信した機体を一覧表示し、機体のラベル（MAC 下位 2 バイト）と照合して選ぶ。近接順（受信強度）に並べる。層 2（講習向け・確実）: コントローラに USB シリアル CLI を追加し、`sf pair` が PC 経由で両機に相手 MAC とチャンネルを書き込む。層 3（任意・後日）: Grove ケーブルで両機を直結して MAC を交換 |
| 効果 | 層 1 で「同時にペアリングモードに入っても取り違えない」を達成（相手は人が選ぶ）。層 2 で「無線に一切依存しない決定的な手順」を講習に提供 |
| 副次効果 | 事後の誤ペア検出（`sf pair status`、画面の MAC 照合）、公開ガイドへの注意書き追加、SILS の 2 組同時ペアリング試験 |

## 1. 現状の事実（調査 2026-09-11）

### 1.1 電文と手順

| 項目 | 事実 | 出典 |
|------|------|------|
| ペアリング電文 | 11 バイト = チャンネル(1) + 機体 MAC(6) + 固定署名(4: AA 55 16 88)。PIN・乱数・機器 ID・受信強度の欄は無い。C++ 側には構造体すら無く定数のみ | `protocol/spec/messages.yaml:428-449`、`firmware/common/protocol/include/espnow_protocol.hpp:45-74` |
| 機体側 | 未ペア時は地上または手持ちで自動的に（2026-09-12: IDLE_HELD からの突入も許容、`pairing_plan.md` 参照）、または本体ボタン 3 秒長押し／CLI `pair` でペアリングモード。500 ms ごとに上記電文を一斉送信。**ペアリング中に最初に届いた操縦電文の送信元 MAC を無条件に相手と確定**し NVS（`sf_pair/ctrl_mac`）に保存。成立後は相手以外の電文を破棄 | `firmware/vehicle/components/sf_comm/comm.cpp:459-497,518-561` |
| コントローラ側 | 電源投入時にボタン押下でペアリングモード。チャンネル 1〜13 を 200 ms ずつ順に切り替え、**署名が一致する最初のペアリング電文の MAC とチャンネルを無条件に採用**し SPIFFS（`peer_info.txt`）に保存 | `firmware/controller/components/espnow_tdma/espnow_tdma.c:129-152,496-546` |
| 選別の仕組み | 受信強度の比較、相手の選択画面、ボタン同時押し確認、PIN のいずれも無い | `pairing_plan.md:111-136` |
| 事後検出 | 自動検出は無い。コントローラ画面の MAC 下位 2 バイトと機体のセット番号ラベルを目視で照合するのみ。誤ペアは「本来の相手が操縦不能」という形で顕在化する | `docs/events/dxh2026/setup-loaner-pc.md:214` |
| チャンネル分散（1/6/11） | 成立後の電波干渉を減らす目的。ペアリング時はコントローラが全チャンネルを走査するため、取り違えの防止にはならない | `pairing_plan.md:127` |

### 1.2 両機のハードと操作手段

| 機器 | 使える入出力 | 備考 |
|------|-------------|------|
| 機体（StampFly） | USB-C（CLI `stampfly>`: `pair` `unpair` `pair status` `param set wifi.channel`）、Grove 端子 ×2（用途・ピン配置は機体仕様書で「要確認」）、本体ボタン（短押し・3 秒・5 秒）、RGB LED、ブザー | 自 MAC を表示する CLI コマンドは無い |
| コントローラ（AtomS3 + Atom JoyStick） | LCD（自チャンネル・機器 ID・相手 MAC 下位 2 バイトを表示）、画面押し込みボタン、黄ボタン ×2、スティック押し込み ×2、USB-C（書き込みと USB ゲームパッドのみ。**対話 CLI は無い**）。Grove の有無は未確認 | 相手 MAC を入力する手段が無い |
| sf CLI | 実機のペアリング・チャンネルを操作するコマンドは無い | `lib/sfcli/commands/` |

### 1.3 文書の状態

公開ガイド（`README.md`、`docs/guides/controller.md`）には「同時に複数組をペアリングすると
取り違える」という注意が無い。注意は開発者向け（`operation_manual.md`、`pairing_plan.md`）と
講習運営用の内部文書（DXH2026）にしか無い。

## 2. 問題の整理

取り違えは「誰が誰と組むか」を決める情報が電文にも操作にも無いことに尽きる。対策は
次の 2 つのどちらか（または両方）を与えることである。

1. **識別子**: 両機が共有する値（ペアコード、宛先 MAC）を電文に載せ、一致しない相手を
   受理しない。
2. **帯域外の経路**: 無線以外（画面での選択、USB、ケーブル）で相手を確定してから無線を
   使う。

どの方式を採っても、最終形は「コントローラが機体 MAC を知り、機体は自分宛だけを受理する
（宛先指定ペアリング）」に収束する。これを共通基盤として先に作り、その上に入力手段を
載せるのが最も手戻りが少ない。

## 3. 方式の比較

### 3.1 無線のみ

| 案 | 仕組み | 長所 | 短所・リスク | 実装規模 |
|----|-------|------|------------|---------|
| W1 近接選択（受信強度） | 両側で受信強度が最大かつ閾値以上（例: 機体をコントローラに接触させた距離）の相手だけを採用。候補が複数近接なら保留して LED で警告 | ハード・操作の追加なし。「近づけて押す」だけ | 隣席が数十 cm で同時にやると破綻。閾値が環境（筐体・電池・机の材質）に依存し、調整が要る。単独では決定的でない | 小（受信コールバックに `rx_ctrl->rssi` を通す、両ファーム） |
| W2 ペアコード（PIN） | 両機で同じ 1〜2 桁のコードを設定し、ペアリング電文と操縦電文に載せる。一致しない相手は無視 | 決定的。座席番号 = コードで運用しやすい。無線だけで完結 | 機体側のコード入力が不便（ボタン押し回数、または USB CLI `pair code N`）。電文の拡張（v2）が必要 | 中（電文 v2、両ファーム、コントローラの入力画面） |
| **W3 コントローラで候補を選ぶ（推奨・層 1）** | コントローラは受信した全ペアリング電文の MAC 下位 2 バイトを受信強度順に画面へ一覧表示し、利用者がスティックで選んで決定。以後は**その MAC 宛**に操縦電文を送る。機体はペアリング中も自分宛（`drone_mac` 一致）の電文だけを受理 | 決定的。追加ハード不要。機体側の操作不要。ラベル照合が手順に組み込まれる。既存の MAC 表示・ラベル運用と整合 | コントローラの画面 UI 実装。機体に MAC ラベルを貼る運用が前提（既にセット番号ラベルあり） | 中（コントローラ UI、機体側は受理条件の変更のみ） |
| W4 ボタン同時押し + 時間窓 | 両機でボタンを同時に押した 2 秒以内の相手だけを採用 | 操作が直感的 | 隣が同じ瞬間に押せば同じ問題。単独では不十分（W1 と組み合わせる前提） | 小 |
| W5 チャンネルで分ける | コントローラ側で先にチャンネルを固定し、走査を固定チャンネルだけにする | グループ数（3）まで分離できる | 同一チャンネル内の同時ペアには無効。1/6/11 の 3 グループが上限 | 小 |

### 3.2 有線（機体とコントローラを直結）

| 案 | 仕組み | 長所 | 短所・リスク | 実装規模 |
|----|-------|------|------------|---------|
| C1 Grove ケーブルで MAC 交換 | 両機の Grove を専用ケーブルでつなぎ、UART で互いの MAC とチャンネルを交換して保存。「挿す → LED 緑 → 抜く」 | 完全に決定的。PC 不要。講習で最も説明が易しい | 機体の Grove ピン配置・電源電圧が未確認、コントローラ側の Grove の有無も未確認。ケーブルの製作・配布。両ファームに UART 交換の実装。誤挿入対策 | 中〜大（ハード確認が先） |
| C2 物理接点（ポゴピン等） | 機体をコントローラに載せると接点で交換 | 手順が最も簡単 | 筐体の改造が要る。本計画では対象外 | 大 |

### 3.3 双方 CLI（PC が仲介）

| 案 | 仕組み | 長所 | 短所・リスク | 実装規模 |
|----|-------|------|------------|---------|
| **P1 コントローラに USB CLI を追加し `sf pair` で書き込む（推奨・層 2）** | コントローラに `controller>` コンソール（`mac`、`peer set <mac> <ch>`、`channel set N`、`peer status`）を実装。機体 CLI に `mac` と `peer set <mac>` を追加。`sf pair` が両機から MAC を読み、互いに書き込んで再起動。USB ポートが 1 つでも挿し替えを対話で誘導 | 無線に一切依存しない。講習は PC 前提なので手順が `sf` に統一される（既存方針）。`sf pair status` で誤ペアを機械的に検出できる | コントローラの USB は現在ゲームパッド（HID）専用で、CDC との同居（複合デバイス）の実装確認が要る。書き込み後の再起動と USB 抜き差しの手間 | 中（コントローラ側の CDC コンソール新設が主） |
| P2 機体の WiFi CLI 経由 | 機体の TCP CLI に `peer set` を追加 | 機体側は無線で済む | コントローラ側に対応する経路が無いので P1 が前提。単独では成立しない | 小（P1 の付属） |

### 3.4 併存

| 案 | 内容 | 評価 |
|----|------|------|
| **M1 = W3 + P1（推奨）** | 既定は W3（ハード不要、1 人で完結）。講習は P1（`sf pair`、決定的）。どちらも共通基盤（宛先指定ペアリング）の上に載る | 2 つの経路が独立しているので、片方が使えない状況（PC 無し／画面が見えない）でももう片方で組める |
| M2 = M1 + W1 | W3 の一覧を受信強度順にするので、事実上 W1 を含む。閾値による自動選択はしない（保留の誤動作を避ける） | 推奨に含める |
| M3 = M1 + C1 | Grove の実態確認後に追加 | 後日判断 |
| M4 = W2 のみ | 電文にペアコードを載せる案だけで済ませる | 機体側の入力が不便で講習向きでない。W3 に劣る |

### 3.5 比較表

| 観点 | W1 近接 | W2 コード | W3 一覧選択 | C1 ケーブル | P1 sf pair | M1 = W3 + P1 |
|------|--------|---------|-----------|-----------|-----------|-------------|
| 同時ペアリングでの決定性 | 低 | 高 | 高 | 高 | 高 | 高 |
| 追加ハード | なし | なし | なし | ケーブル | USB ケーブル（既存） | なし |
| PC の要否 | 不要 | 不要 | 不要 | 不要 | 必要 | 既定は不要 |
| 機体側の操作 | ボタン | コード入力 | 不要 | 挿すだけ | USB 接続 | 不要／USB |
| 電文の変更 | なし | v2 | v2（宛先） | なし（別経路） | なし（別経路） | v2 |
| 実装規模 | 小 | 中 | 中 | 中〜大 | 中 | 中 + 中 |
| 未確認のリスク | 閾値 | なし | なし | Grove 仕様 | CDC + HID 同居 | CDC + HID 同居 |

## 4. 実装設計（決定版: W3 のみ）

### 4.1 機体側（firmware/vehicle）

| 項目 | 変更 |
|------|------|
| 受理条件 | ペアリング中（`PairingState::Pairing`）に届く操縦電文のうち、`drone_mac[0..2]` が自 MAC の下 3 バイトと一致するものだけを相手候補にする。一致しない電文（隣のコントローラが別の機体を選んだもの）は無視。成立後のフィルタ（相手 MAC 以外を破棄）は従来どおり |
| ペアリング電文 | 変更なし（500 ms ごとの一斉送信、内容は チャンネル + 自 MAC + 署名） |
| CLI `mac` | 新設。USB モニタ接続（`sf monitor`）で自 MAC を表示する。1 行目に完全な MAC（`XX:XX:XX:XX:XX:XX`）、2 行目に**ラベル用の下 4 桁**（例 `A1B2`）を出す |
| CLI `pair status` | 自 MAC（下 4 桁併記）と、バインド済みなら相手 MAC を表示する |
| 起動ログ | 起動時に自 MAC を 1 行出力する（モニタを開いたまま電源を入れれば分かる） |
| 設計文書 | `pairing_plan.md` の P4 に本方式を「実装済み」として記録。`espnow_tdma.yaml` の `multi_vehicle.collision_handling` を更新 |

### 4.2 コントローラ側（firmware/controller）

| 項目 | 変更 |
|------|------|
| 候補の収集 | ペアリングモードでチャンネル 1〜13 を従来どおり走査し、署名一致のペアリング電文を「最初の 1 通で確定」せず、送信元 MAC・チャンネル・受信強度を候補表（最大 8 件、同一 MAC は更新）に貯める。走査は利用者が決定するまで繰り返す |
| 画面 | LCD に候補を受信強度の強い順に表示。各行は「MAC 下 4 桁」と「CH」（受信強度は目安の記号）。行を選ぶとその行を反転表示。候補が 0 件のときは「Searching…」 |
| 操作 | スティック上下（または黄ボタン）で行を移動、画面押し込みボタンで確定。**候補が 1 件でも確定操作を要求する**（隣の機体しか見えていない場合の誤確定を防ぐ）。ペアリングモードからの離脱は電源再投入 |
| 確定後 | 選んだ MAC・チャンネルを SPIFFS（`peer_info.txt`）に保存し、その MAC をユニキャスト相手として登録。以後の操縦電文の `drone_mac` に選んだ MAC の下 3 バイトを入れる（既存の動作を確認して維持） |
| 成立の確認 | 機体からのテレメトリ（送信元 = 選んだ MAC）を受信した時点で成立と表示。一定時間（例 5 秒）受信できなければ「機体側がペアリングモードか確認」と表示して候補一覧に戻る |
| 受信強度 | ESP-NOW 受信コールバックの `rx_ctrl->rssi` を使う。取得できない IDF 版なら順序は受信順にし、強度表示を省く（機能は成立する） |

### 4.3 見送った案（記録）

W1 近接自動選択、W2 ペアコード、W4 同時押し、C1 Grove ケーブル、P1 コントローラ CLI +
`sf pair` は今回見送り。比較は §3 を参照。将来必要になれば §4.1 の受理条件（自分宛だけ受理）は
そのまま土台になる。

### 4.4 検証

| 段階 | 内容 |
|------|------|
| SILS | 仮想コントローラ 2 台・機体 2 台を同時にペアリングモードにし、片方のコントローラが別の機体の MAC を宛先に指定して送っても、機体がそれにバインドしないことを試験する（機体側の受理条件の試験。SILS の仮想コントローラが `drone_mac` を指定できるかを最初に確認し、できなければ拡張する）。既存の `pairing.scn` は従来どおり PASS |
| 実機（ベンチ） | 2 組を 30 cm 以内で同時にペアリング。両コントローラの画面に 2 件表示され、それぞれ自分の機体（ラベル照合）を選ぶと正しい組になる。単機でも従来と同じ手順（電源投入時ボタン押下 → 1 件表示 → 確定）で成立する |
| 講習リハーサル | 10 組同時。手順書どおりに 5 分以内に全組が成立し、誤ペアがゼロ |

## 5. 段階計画

| Phase | 内容 | 合格基準 | 状態 |
|-------|------|---------|------|
| 1 機体側 | 受理条件の変更、CLI `mac`、`pair status` の自 MAC 表示、起動ログ、SILS 試験、`sf build vehicle` | SILS の新試験と既存 `pairing.scn` が PASS。ビルド成功 | **完了**（コミット `79e2c7c8`。SILS 29 PASS + 5 KNOWN-FAIL + 1 SKIP、FAIL 0。host unit test 35/35） |
| 2 コントローラ側 | 候補表・画面・操作・確定・成立確認、`sf build controller` | ビルド成功。実機ベンチ 2 組同時で誤ペアゼロ（実機は次回） | **完了**（コミット `88c46806`。`sf build controller` OK。実機 2 組同時ペアリングの検証は次回） |
| 3 文書・運用 | `docs/guides/controller.md`・`README.md`・`operation_manual.md` の手順更新と取り違えの注意書き、講習ランシート、機体ラベルに MAC 下 4 桁を併記する運用、`pairing_plan.md` P4 の更新 | 公開ガイドに新手順（一覧から選ぶ）と注意書きがある | **進行中**（本セッションで実施。変更したドキュメントは下記） |

### Phase 3 で変更したドキュメント

| ファイル | 変更内容 |
|---------|---------|
| `docs/guides/controller.md` | §7 ペアリング手順を新方式（一覧選択・確定操作必須）に全面改稿（JA/EN）。「取り違え防止」節を新設。トラブルシューティング表を更新 |
| `README.md` | クイックスタートのペアリング手順（③）を新方式に更新（JA/EN）。`mac` コマンドとラベルに言及 |
| `firmware/vehicle/docs/operation_manual.md` | §5 ペアリング手順を新方式に全面改稿（JA/EN）。CLI コマンド表に `mac` を追加。`pair status` の出力例を追加。「1ペアずつ順番に」の注意書きを新しい注意（複数組同時可・ラベル照合必須・LED 緑が最終確認）に置換 |
| `docs/guides/motor_spin_quickstart.md` | §7 ペアリング手順に一覧選択・確定操作のステップを追加 |
| `docs/events/sci_tutorial_2026/instructor_runsheet.md` | 事前チェックリスト・実習1(2/2)・実習4の各行を新方式に更新（JA/EN）。ラベル準備の注記を追加 |
| `docs/events/sci_tutorial_2026/cheatsheet.md` | ペアリング手順の1文を新方式に更新（JA/EN） |
| `docs/events/sci_tutorial_2026/verification_checklist.md` | ベンチ確認のペアリング項目を新方式に更新（JA/EN） |
| `docs/events/sci_tutorial_2026/README.md` | 「機体・コントローラの準備」手順2を新方式に更新（JA/EN） |
| `docs/events/dxh2026/setup-loaner-pc.md` | §2-3「ペアリング（1セットずつ）」を「複数セット同時可・ラベル照合必須」に全面改稿。§4-2 見出し・本文を同様に更新 |
| `docs/events/dxh2026/equipment-checklist.md` | 前日チェックリスト・終了後チェックリストのペアリング/再ペアリング項目を新方式に更新 |
| `docs/events/dxh2026/curriculum.md` | リスク対応表「クロスペアリング」行を新方式の仕組みの説明に更新。§4-2 参照文言の「1セットずつ」を削除 |
| `docs/events/stampfly_workshop/slides/chapters/environment_setup.tex` | 変更なし（「ペアリングして使用する」という一般的な言及のみで、旧ルールの明記なし。確認のみ） |
| `docs/events/stampfly_workshop/slides/chapters/controller_input.tex` | 「教室では一組ずつペアリングする」という旧ルールの明記を発見。手順表に候補一覧からの選択ステップを追加し、注意書きを新方式の説明に置換（**PDF は未リビルド**、本計画の指示どおりテキストのみ最小修正） |

## 6. 決定事項の記録

| # | 論点 | 決定 |
|---|------|------|
| 1 | 採用方式 | W3 のみ（2026-09-11 ユーザー決定）。他は見送り |
| 2 | 電文 | 変更しない（既存の情報で足りる） |
| 3 | 機体側の変更 | 自分宛の操縦電文だけを相手候補にする + CLI `mac`（ユーザー指示 2026-09-11） |
| 4 | 候補が 1 件のときの扱い | 自動確定せず確定操作を要求する（隣の機体しか見えていない場合の誤確定を防ぐ）。運用上の負担が大きければ後日「1 件なら 3 秒で自動確定」に変更可 |
| 5 | 機体ラベル | MAC 下 4 桁を併記する（`mac` コマンドで読み取って印字） |

## 7. 未確認事項

- コントローラ側の ESP-NOW 受信コールバックで受信強度が取得できるか（IDF 版依存。取れなければ受信順表示）。
- コントローラ LCD で候補 8 件を読める文字サイズ（実機で確認。5 件に減らす可能性）。
- SILS の仮想コントローラが操縦電文の `drone_mac` を指定できるか。
- 実機 2 組同時ペアリングの動作（次回の実機セッション）。
