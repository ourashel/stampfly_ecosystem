# ====================
# OS
# ====================
.DS_Store
.DS_Store?
._*
Thumbs.db

# ====================
# IDE / Editor
# ====================
.vscode/
.idea/
*.swp
*.swo
*~

# ====================
# ESP-IDF
# ====================
**/build/
sdkconfig
sdkconfig.old
dependencies.lock
managed_components/

# ====================
# Python
# ====================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
.env
*.egg-info/
dist/
build/
.eggs/
*.egg
# PyInstaller-generated spec file (e.g. from building the installer/flasher
# GUIs with `pyinstaller --onefile ...`); regenerated on every build, not
# hand-maintained, so it should not be committed.
# PyInstaller が生成する spec ファイル(インストーラ/フラッシャ GUI を
# `pyinstaller --onefile ...` でビルドした際に生成される)。ビルドの度に
# 再生成され手動管理しないため、コミットしない。
*.spec
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/

# ====================
# StampFly Ecosystem
# ====================
# Local configuration (generated by installer)
.sf/
# Legacy files
.esp-idf/
.sfconfig.json

# ====================
# Analysis outputs
# ====================
analysis/reports/*
!analysis/reports/.gitignore
# Model-match gate reference (SSOT for sf sils sysid-gate) — keep in the repo
# モデル一致ゲートの実機基準値（sf sils sysid-gate の SSOT）— リポジトリに含める
!analysis/reports/rate_sysid_reference/

# ====================
# Logs
# ====================
*.log
logs/*
!logs/.gitkeep
!logs/README.md

# Log analyzer data files (keep scripts, ignore data)
tools/log_analyzer/*.csv
tools/log_analyzer/*.bin
tools/log_analyzer/*.png

# ====================
# Sample datasets (regenerable from real flights — keep README only)
# ====================
analysis/datasets/education/*.csv
analysis/datasets/education/*.sflog.zip
analysis/datasets/sysid/*.csv

# ====================
# Compiled
# ====================
*.o
*.a
*.so
*.elf
*.bin
!firmware/legacy/*.bin
*.hex
*.map

# ====================
# LaTeX build artifacts
# ====================
*.aux
*.fdb_latexmk
*.fls
*.nav
*.out
*.snm
*.toc
*.vrb
*.synctex.gz

# MkDocs build output
site/

# Personal notes
knote/

# Work in progress (not ready for git)
# 開発中（git管理対象外）
_wip/

# Private documents (conference manuscripts, grant applications, personal plans)
# are kept outside this public repository. Never add them here, even on a branch.
# 非公開文書（学会原稿・申請書・個人計画）はこの公開リポジトリの外で管理する。
# ブランチであってもここに追加しない（クラウド版セッションは作業ブランチを origin へ push する）。
docs/sci26/
docs/juida2026/
docs/hikoki64/
docs/si2026/
docs/kakenhi/
docs/_private/
papers/
grants/
analysis/scripts/verify_hikoki64/

# ====================
# Build / runtime caches
# ====================
**/.cache/

# ====================
# Beamer auto-generated listings
# ====================
docs/events/*/slides/*.listing

# ====================
# Temporary / WIP firmware projects
# ====================
firmware/my_drone/

# ====================
# Local binary backups
# ローカルバイナリバックアップ
# ====================
*.bak.bin

# E6 scenario run bundles (regenerated by `sf sils scenario`)
simulator/sils/viz/out_scn_*/

# P6 stage 1: `sf sils fly` session bundle (trajectory + param overrides,
# regenerated every session — see simulator/sils/README.md's `sf sils fly` section)
simulator/sils/viz/out_fly/

# Generated analysis artifacts (figures, metrics)
analysis/out/

# Claude Code worktrees (作業用チェックアウト実体 / working checkouts)
.claude/worktrees/
